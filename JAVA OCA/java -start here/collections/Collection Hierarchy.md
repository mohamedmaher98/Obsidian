بص بقى، الـ **Java Collection Hierarchy** عاملة زي شجرة عيلة كده، وكل فرع فيها له شخصية مختلفة.

## 🌳 القمة — الـ Interfaces الأساسية

-  ال **`Iterable`**  
    ده الجد الكبير، أي حاجة ممكن تمشي عليها بـ `for-each` loop لازم تورث منه.  
    ⬇️ تحته:
    
- ال **`Collection`**  
    ده بيمثل مجموعة عناصر (Elements)، وبيوفر عمليات زي `add`, `remove`, `size`.
    
    ---
    ## 🌱 الفروع الأساسية من Collection

1. ل**`اList`** (مرتبة + بيسمح بالتكرار)
    
    - ال`ArrayList` (أسرع في القراءة، أبطأ شوية في الحذف/الإضافة في النص)
        
    - ال`LinkedList` (أسرع في الإضافة/الحذف في النص، أبطأ في الوصول العشوائي)
        
    - `الVector` (قديم، متزامن Thread-safe، بطيء غالبًا)
        
2.ال **`Set`** (مفيش تكرار + غالبًا مش مرتبة)
    
    -ال `HashSet` (أسرع، مفيش ترتيب)
        
    - ال`LinkedHashSet` (يحافظ على ترتيب الإدخال)
        
    - ال`TreeSet` (مرتبة طبيعيًا أو حسب Comparator)
        
2. **`الQueue`** (طابور - First In First Out)
    
    - `الPriorityQueue` (بيحافظ على الأولوية مش الترتيب العادي)
        
    - `الArrayDeque` (طابور مزدوج - ممكن تضيف وتمسح من الأول أو الآخر)
        

---

## 🌵 فرع تاني بره Collection
- ال**`Map`** (Key -> Value)  
    مش بيورث من `Collection`، بس جزء مهم في العيلة.
    
    -ال `HashMap` (أسرع، مفيش ترتيب)
        
    - ال`LinkedHashMap` (يحافظ على ترتيب الإدخال)
        
    - ال`TreeMap` (مرتبة حسب المفتاح)
        
    - ال`Hashtable` (قديم + Thread-safe)





      
      
      ![[Pasted image 20250813163805.png]]
  