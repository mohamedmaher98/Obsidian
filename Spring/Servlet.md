==Servlet== 

كلاس Java بيشتغل جوه سيرفر ويب (زي Tomcat)، وبيتعامل مع الـ HTTP Requests & Responses.
لازم يكون شغال على **سيرفر تطبيقات** (Application Server) أو **سيرفر ويب** زي:
- Tomcat
- Jetty
- WildFly
- لما المستخدم يفتح لينك في المتصفح، السيرفر يبعت الطلب (request) للسيرفلت، والسيرفلت يعالج الطلب ويرجع رد (response).
- http://localhost:8080/hello
- السيرفلت ينفذ كود Java زي:
```java
@WebServlet("/hello")
public class HelloServlet extends HttpServlet {
    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp)
            throws ServletException, IOException {
        resp.setContentType("text/html");
        PrintWriter out = resp.getWriter();
        out.println("<h1>أهلاً بيك في أول سيرفلت!</h1>");
    }
}```

![[Pasted image 20250509180642.png]]

السيرفلت تقدر تستقبل اكتر من ريكويست علي نفس السيرفر عن طريق المالتي ثريد 
فالسيرفلت معتمد علي المالتي ثريد فبيهندل اكتر من ريكوست فنفس الوقت ودي الميزه بتاعته عن الCGI

![[Pasted image 20250509180941.png]]
![[Pasted image 20250509181047.png]]

 السيرفيلت عبارة عن كلاس جافا عادي بيبقي معموله اكتيند من HttpServelt وده عشان اقدر استخدم الدوال بتاعت ال init()  , destroy , service()
 init دي بتقوم اول ما بيقوم السيرفلت لما بكون عا وز اعرف فيها حات مع اول ريكوست بيتعمل اول اول ما الريكوست بيتبعت
 ال destroy  ميثود دي بعمل فيها الحاجات الي عاوزها تتمعمل لما السيرفلت ينتهي او يخلص زي هقفل كونكشن مفتوح او حاجه 

---
### ==**Static and Dynamic Web site**== 

 ## الفرق بين Static و Dynamic Website

| العنصر                 | Static Website            | Dynamic Website                       |
| ---------------------- | ------------------------- | ------------------------------------- |
| 🔄 تغيير المحتوى       | ثابت                      | متغير حسب المستخدم أو البيانات        |
| 🧠 لغة البرمجة         | HTML, CSS, JavaScript فقط | + لغة سيرفر (PHP, Java, Python...)    |
| 🗃️ قاعدة بيانات       | لا تحتاج                  | تحتاج غالبًا                          |
| ⚙️ الأداء              | أسرع                      | أبطأ نسبيًا                           |
| 👨‍🔧 التحديث          | يدوي                      | تلقائي                                |
| 📦 التكلفة             | أقل                       | أعلى                                  |
| 🧩 الاستخدامات الشائعة | مواقع بسيطة               | تطبيقات معقدة ومتاجر إلكترونية وغيرها |
ما هو HTTP
**HTTP** هو اختصار لـ **HyperText Transfer Protocol**، وهو البروتوكول المسؤول عن التواصل بين **المتصفح (العميل)** و**السيرفر (الخادم)**.
HTTP Request
هو **الطلب** اللي المتصفح أو الكلاينت بيبعته للسيرفر علشان ياخد بيانات أو يبعت بيانات.
 يتكون من:

1. **Method (الطريقة)**: زي `GET`, `POST`, `PUT`, `DELETE`
    
2. **URL (المسار)**: الرابط اللي عايز توصله
    
3. **Headers (رؤوس الطلب)**: معلومات زي نوع البيانات، اللغة، التوكين، إلخ
    
4. **Body (جسم الطلب)**: لو `POST` أو `PUT`، بيكون فيه بيانات (زي JSON أو form-data)
==Content-Type==

   هو نوع البيانات اللي موجودة في جسم الـ Request أو Response.
	   text/html	     :                                                                            صفحة HTML
	application/json	                                                                            بيانات بصيغة JSON (زي {"name":"Ali"})
	application/x-www-form-urlencoded	                                  فورم زي اللي بتتبعت من نموذج HTML عادي
	multipart/form-data                                                                 	بيستخدم في رفع ملفات (form مع ملف)
	
 ==Request Container==
 

**الـ container** في السياق ده غالبًا بيشير للـ **Web Container** أو **Servlet Container** زي Tomcat أو Jetty، وده هو المكان اللي بيشغّل الـ Java Web Application.
 دوره:
- يستقبل الـ HTTP Request.
- يمرّره للـ Servlet أو الـ Controller.
- بعدين يرجّع الـ HTTP Response للعميل.
==ملخص== 
## ==HTTP & HTTP Request & Content-Type==

- **HTTP**: بروتوكول التواصل بين العميل والسيرفر.
- **HTTP Request**: طلب بيبعته العميل للحصول على أو إرسال بيانات.
  - Methods: GET, POST, PUT, DELETE
  - يحتوي على: URL + Headers + Body
- **Content-Type**: نوع البيانات داخل الـ Request أو Response.
  - `application/json`: بيانات JSON
  - `text/html`: صفحة HTML
  - `multipart/form-data`: فورم مع ملفات
- **Web Container (مثلاً Tomcat)**:
  - يستقبل ويعالج HTTP Requests
  - يوجّهها للـ Controllers أو Servlets
---
### ==**الفرق مابين Get and Post**==
## الفرق بين GET و POST في HTTP

| الخاصية               | GET                                        | POST                                       |
|------------------------|---------------------------------------------|---------------------------------------------|
| الغرض                 | جلب البيانات                                | إرسال أو تعديل البيانات                     |
| مكان البيانات         | في الرابط (URL)                             | في جسم الطلب (Body)                         |
| الحد الأقصى للبيانات | محدود (2048 حرف تقريبًا)                   | غير محدود عمليًا                            |
| الأمان               | أقل أمانًا – يظهر في الرابط                 | أكثر أمانًا – البيانات مخفية                |
| التخزين (Cache)      | ممكن التخزين                                | لا يتم التخزين                              |
| إعادة الإرسال         | آمن في إعادة الإرسال                        | خطر التكرار (مثلاً: دفع مرتين)              |
| أمثلة الاستخدام       | استعلامات بحث – تصفح صفحات                 | تسجيل دخول – إضافة/تعديل بيانات             |

 🔁 الفرق بين GET و POST في HTTP
يعني امتي استخدم ده وامتي ده 

| المقارنة                                                                                  | GET                                                                                               | POST                                       |     |
| ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------ | --- |
| النوع                                                                                   . | قراءة بيانات فقط (Retrieve)                                                                       | إرسال أو تعديل بيانات (Send/Modify)        |     |
| عرض البيانات في الرابط                                                                    | ✅ نعم (في الـ URL)                                                                                | ❌ لا، البيانات في Body الطلب               |     |
| الحجم الأقصى للبيانات                             .                                       | محدود (يعتمد على المتصفح والسيرفر)                                                              . | كبير (يدعم إرسال ملفات وبيانات كثيرة)      |     |
| الكاش (Cacheable)                                                                         | ✅ غالبًا بيتخزن في الكاش                                                                          | ❌ لا يُخزن غالبًا                          |     |
| الاستخدام                                                                                 | جلب بيانات (زي عرض مقال أو قائمة)                                                                 | تسجيل بيانات (زي تسجيل دخول أو إضافة منتج) |     |
| الأمان                                                                                    | ❌ أقل أمان لو فيه بيانات حساسة                                                                    | ✅ أكثر أمان لنقل بيانات حساسة              |     |
| يمكن تكراره بسهولة                                                                        | ✅ نعم (Refresh أو Bookmark)                                                                       | ❌ لا، ممكن يسبب مشاكل لو أُعيد إرساله      |     |

> 📝 قاعدة عامة:  
> استخدم `GET` للقراءة فقط،
>  و`POST` لأي عملية تعديل أو إرسال بيانات.


---
### ==**Web Server  Vs Application Server**== 

Web Server : 
هو البرنامج أو الجهاز اللي بيستقبل طلبات HTTP من المتصفح ويرد عليها بصفحات HTML أو بيانات.
Web Application	:
هي البرنامج نفسه اللي بيشتغل على الويب وبيعالج البيانات والمنطق وبيتعامل مع المستخدم.
- **Web Server**:
- مسؤول عن استضافة وتشغيل ملفات الموقع (HTML/CSS/JS)، وأحيانًا بيمرر الطلبات للـ Web Application.
    
- **Web Application**:
- هو النظام البرمجي اللي فيه منطق البزنس (زي تسجيل الدخول، الشراء، إضافة منتجات... إلخ).
- ## 🌐 Web Server vs Web Application

| المقارنة            | Web Server                                     | Web Application                                |
|----------------------|------------------------------------------------|-------------------------------------------------|
| الوظيفة الأساسية     | استقبال طلبات HTTP وتقديم صفحات               | تنفيذ منطق البزنس والتعامل مع البيانات          |
| النوع                | برنامج أو جهاز (زي Apache، Nginx)              | برنامج/مشروع معمول بلغة زي Java أو PHP أو Python |
| المحتوى              | ملفات HTML، CSS، JS ثابتة                      | صفحات ديناميكية تتفاعل مع المستخدم والبيانات     |
| يتفاعل مع قاعدة البيانات؟ | ❌ لا مباشرة                                 | ✅ نعم                                           |
| مثال                 | Apache، Nginx                                  | Spring Boot، Laravel، Django                   |
| مكان التشغيل         | على السيرفر                                    | يعمل من خلال Web Server                         |

> 📌 خلاصة:  
> Web Server
>  بيشغل ويخدم ملفات،
>   أما Web Application بينفذ الوظائف والمنطق اللي في الموقع.

---
### ==**Session**==
دي عبارة عن جلسه للمستخدم وفيها معلومات زي وقت الدخول ومدتها ووقت الخروج وبيسجل المعلومات دي
فانا بحتاجها عشان لا ال http is stateless  يعني لو خلصت الريكوست بتاعك وعملت واحد جديد هقو مايعرفش مين الي باعت وكده
في جافا في انتر فيس اسمها HttpSession   لكل يوزر بيبقي ليه session  خاصه بيه
لما المستخدم يدخل أول مرة، السيرفر بيبدأ له Session جديدة ويدي له **Session ID** (غالبًا بيتخزن في الكوكيز). بعد كده، كل ما يبعت طلب جديد، السيرفر يقدر يعرفه باستخدام الـ ID ده ويجيب بياناته.
عشان اضيف بينات لل session 
```java
protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
    HttpSession session = request.getSession(); // تبدأ أو تسترجع الجلسة
    String username = request.getParameter("username");
    session.setAttribute("user", username); // تخزين البيانات
    response.getWriter().println("Username saved in session: " + username);
}
// عشان اقرا البينات من السيشن 
protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
    HttpSession session = request.getSession(false); // false = ما يعملش Session جديدة لو مش موجودة
    String username = (String) session.getAttribute("user");

    if (username != null) {
        response.getWriter().println("Welcome back, " + username);
    } else {
        response.getWriter().println("No session found.");
    }
}
// عشان اتحكم في مدة الجلسه 
session.setMaxInactiveInterval(60 * 30); // 30 دقيقة
// عشان احذف السيشن 
session.invalidate(); // ينهي الجلسة نهائيًا

```

| العنصر                     | الشرح                                                                |
|----------------------------|----------------------------------------------------------------------|
| `HttpSession`              | كائن يمثل الجلسة الخاصة بالمستخدم                                   |
| `getSession()`             | ينشئ أو يرجع Session موجودة بالفعل                                  |
| `setAttribute(key, value)` | يخزن بيانات في الجلسة                                               |
| `getAttribute(key)`        | يسترجع البيانات المخزنة                                             |
| `invalidate()`             | يمسح الجلسة تمامًا                                                  |
| `setMaxInactiveInterval()` | يحدد وقت انتهاء الجلسة في حالة عدم النشاط                           |

---
### **==Cookies In Servlet==**

**الـ Cookie** هي "ملف صغير" بيتم تخزينه في جهاز المستخدم من خلال المتصفح، وبيحتوي على معلومات معينة بيبعته كل مرة مع أي طلب جديد بيتم إرساله لنفس السيرفر.

> يعني الكوكي بتتخزن **عند العميل (Client)**  
> لكن الـ Session بتتخزن **عند السيرفر (Server)**

استخداماتها 
- تذكر اسم المستخدم.
- تخزين إعدادات مفضّلة.
- متابعة المستخدم بدون الحاجة لتسجيل دخول كل مرة.
```java
protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
    String username = request.getParameter("username");

    Cookie userCookie = new Cookie("user", username); // إنشاء الكوكي
    userCookie.setMaxAge(60 * 60 * 24); // يوم كامل
    response.addCookie(userCookie); // إرسال الكوكي للمتصفح

    response.getWriter().println("Cookie created for user: " + username);
}

عشان اقرا الكوكيز 
protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
    Cookie[] cookies = request.getCookies();
    String username = null;

    if (cookies != null) {
        for (Cookie cookie : cookies) {
            if ("user".equals(cookie.getName())) {
                username = cookie.getValue();
                break;
            }
        }
    }

    if (username != null) {
        response.getWriter().println("Welcome back, " + username);
    } else {
        response.getWriter().println("No user cookie found.");
    }
}
حذف الكوكيز
Cookie userCookie = new Cookie("user", null);
userCookie.setMaxAge(0); // عمره صفر يعني يتحذف
response.addCookie(userCookie);

```

| العنصر                     | الشرح                                                              |
|----------------------------|--------------------------------------------------------------------|
| `Cookie`                   | كائن يمثل الكوكي                                                  |
| `new Cookie(key, value)`   | إنشاء كوكي جديدة                                                  |
| `setMaxAge(seconds)`       | تحديد مدة صلاحية الكوكي                                           |
| `addCookie(cookie)`        | إرسال الكوكي للمتصفح                                              |
| `request.getCookies()`     | استرجاع جميع الكوكيز المُرسلة من المتصفح                          |
| `cookie.getName()`         | الحصول على اسم الكوكي                                             |
| `cookie.getValue()`        | الحصول على القيمة                                                 |
| `setMaxAge(0)`             | حذف الكوكي                                                         |


| العنصر           | Session                          | Cookie                           |
|------------------|----------------------------------|----------------------------------|
| مكان التخزين     | عند السيرفر                      | عند العميل (المتصفح)            |
| الأمان           | أكثر أمان                        | أقل أمان                         |
| الحجم المسموح    | غير محدود تقريبًا                | حوالي 4 KB لكل Cookie           |
| وقت الانتهاء     | تلقائي أو بعد مدة inactivity     | حسب ما يتم تحديده في setMaxAge  |
| الاعتماد على ID  | Yes (Session ID)                 | No (القيمة بتتبعت صريحة)        |
