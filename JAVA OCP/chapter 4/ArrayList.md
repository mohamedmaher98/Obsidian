لو الـ **Array = عمارة ثابتة**  
فالـ **ArrayList = كومباوند بيتوسع لوحده**.

- تضيف شقق؟ يكبر لوحده
    
- تشيل شقق؟ يصغّر
    
- تدور؟ يسهّل
    
- تمسح؟ ينظّف مكانها
    

_هو Flexibility جامدة جدًا فوق الـ Arrays._

# 📌 **Understanding Arrays & ArrayList in Java**

## #️⃣ **1. Arrays**

### ✔ التعريف

الـ **Array** هي بنية بيانات ثابتة الحجم، بتخزن عناصر من **نفس النوع**، وبنستخدم **index** للوصول للعناصر.

---

### 🟦 الخصائص الأساسية

- **Fixed Size**: الحجم ثابت لا يتغير.
    
- **Indexed Access**: الوصول عبر `0 → length-1`.
    
- **Ordered**: العناصر مرتبة.
    
- **Homogeneous**: كل العناصر من نفس النوع.
    

---

### 🧪 مثال إنشاء Array

`int[] numbers = new int[3]; // Array بحجم ثابت int[] values = {10, 20, 30}; // Array بقيم جاهزة`

---

### 🔎 الوصول والتعديل

`numbers[0] = 5; int x = numbers[0];`

---

### 🚨 خطأ شائع

**ArrayIndexOutOfBoundsException**  
يظهر عند محاولة الدخول لـ index خارج الحدود:

`numbers[5] = 10; // ❌`

---

### 🔁 Loop

`for (int i = 0; i < numbers.length; i++) {     System.out.println(numbers[i]); }  for (int n : numbers) {     System.out.println(n); }`

---

---

# #️⃣ **2. ArrayList**

### ✔ التعريف

**ArrayList** = Array مرنة بتكبر وبتصغر لوحدها، وتوفر أدوات سهلة للإضافة والحذف والبحث.

---

### 🟦 كيفية إنشائها

`import java.util.ArrayList;  ArrayList<String> names = new ArrayList<>();`

---

### ➕ إضافة عناصر

`names.add("Ali"); names.add("Sara");`

---

### 🔎 الوصول للعناصر

`String n = names.get(0);`

---

### ✏ تعديل عنصر

`names.set(1, "Omar");`

---

### ❌ حذف عنصر

`names.remove(0);        // حذف بالرقم names.remove("Ali");    // حذف بالاسم`

---

### 📏 معرفة الحجم

`names.size();`

---

### 🧹 مسح كل العناصر

`names.clear();`

---

### 🔁 Loop على الـ ArrayList

`for (String name : names) {     System.out.println(name); }`

---

---

# #️⃣ **3. Array vs ArrayList (Interview Ready)**

|المقارنة|Array|ArrayList|
|---|---|---|
|الحجم|ثابت|مرن|
|الإضافة/الحذف|صعب|سهل جدًا|
|السرعة|أسرع|أبطأ قليلًا|
|يقبل Primitive؟|نعم|لا (Object فقط)|
|الاستخدام|بيانات ثابتة|بيانات بتتغير|

---

# #️⃣ **4. Autoboxing & Unboxing**

لأن ArrayList بتقبل **Objects فقط**:

- `int` → `Integer`
    
- `double` → `Double`
    
- `boolean` → `Boolean`
    

مثال:

`ArrayList<Integer> nums = new ArrayList<>(); nums.add(10); // Autoboxing من int → Integer`

---

# 🎯 **الخلاصة**

- **Array** = ثابتة، سريعة، باستخدام index، مناسبة لما البيانات عددها معروف.
    
- **ArrayList** = مرنة، قوية، سهلة الإضافة والحذف، مناسبة لمعظم التطبيقات.