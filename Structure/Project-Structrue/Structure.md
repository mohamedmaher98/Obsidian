![[Pasted image 20250417112429.png]]
-خاص بال maven  
Maven 
- هي اداة خاصه لادارة وبناء المشاريع في لغة الجافا (kotlen - scala)
  - يدير ال depndance 
  - بيعمل اوامر ال clean - install - test -compile
  - يبني المشروع (بيحول الكود بتاعك لملفات .jar .war )
  - ![[Pasted image 20250417113230.png]]
  ***Pom*** 
- هي اختصار ل project Object Model 
- هي عبارة عن ملف .xml اسمه pom.xml بيكون جوه المشروع وده الي ملف الmaven  بيقرا منه كل التفاصيل الخاصه بالمشروع
- 
دور `pom.xml` في المشروع
1. **تعريف المشروع**: الاسم، الإصدار، الـ groupId والـ artifactId.
    
2. **المكتبات (Dependencies)**: أي مكتبة محتاجها (زي Spring أو Servlet أو Gson...) بتحطها هنا، وMaven هيجيبها تلقائي.
    
3. **الإضافات (Plugins)**: لو محتاج تبني المشروع، تعمل deploy، أو تستخدم أدوات معينة.
    
4. **التنظيم**: لو فيه sub-modules في المشروع.
    
5. **البيئة**: ممكن تحدد إصدار الجافا، encoding، وغيره.
   _____________________________________________________________________________________________


src --> source 
في مشروع Java بيكون `src` هو المجلد اللي فيه كل كود البرنامج:
![[Pasted image 20250417114254.png]]----
----
---
## ملفات `.jar` و `.war`؟

هما **ملفات أرشيف مضغوطة** (زي ZIP)، لكن مخصوصة للجافا، وتستخدم لتجميع الكود والملفات الجاهزة للتشغيل أو النشر.

ملف `.jar` = Java ARchive
> ✅ يستخدم لتجميع كود Java الجاهز للتشغيل كـ _تطبيق عادي (Standalone Application)_

### يتكون من:

- ملفات `.class` (الكود المترجم)
    
- ملف `META-INF/MANIFEST.MF` ← يحتوي على معلومات زي اسم الكلاس الرئيسي Main class
    
- مكتبات إضافية (لو محتاجها)

## ملف `.war` = Web Application ARchive

> ✅ يستخدم لتجميع تطبيق ويب كامل عشان يتنشر على سيرفر (زي Tomcat)

### يتكون من:

- فولدر `WEB-INF/` ← أساسي، وفيه:
    
    - `web.xml` أو إعدادات Spring
        
    - الكلاسات (`classes/`)
        
    - المكتبات (`lib/`)
        
- ملفات JSP
    
- HTML, CSS, JS

- أي حاجة تخص الواجهة


----------

  
  ## مثال كامل لو هتعمل مشروع اسمه `maher-webapp`

| الحقل      | القيمة                                                                     |
| ---------- | -------------------------------------------------------------------------- |
| groupId    | `com.maher`--اسم الشركة                                                    |
| artifactId | -- اسم المشروع نفسه `maher-webapp`                                         |
| version    | -- رقم النسخه وسناب شوت معاناها ان المشروع ده لسه فيه شغل `0.0.1-SNAPSHOT` |
| package    | ده الي هو المكان الي هيتحط فيه المشروع  `com.maher.webapp`                 |
|            |                                                                            |

---
## 🎯 خلاصة الفرق:

|الجزء|محتواه|مثال|
|---|---|---|
|`src/main/java`|كود الجافا اللي انت بتكتبه|`LoginServlet.java`|
|`src/main/resources`|ملفات بيساعد الكود يشتغل (مش Java)|`application.properties`, `log4j.properties`|
