

## 📝 تعريف بسيط
في Java، الكائن (object) هو نسخة حقيقية من الكلاس اللي انت عرفته.  
يعني الكلاس هو "القالب"، والـ object هو "المنتج الفعلي" اللي بيتخزن في الذاكرة وبتقدر تتعامل معاه.

---

## 🧱 إزاي بننشئ كائن؟
بنستخدم الكلمة المفتاحية `new`:

```java
ClassName objName = new ClassName();
```

📌 المعنى:

- `ClassName` → اسم الكلاس.
    
- `objName` → اسم الكائن اللي انت بتسميه.
    
- `new` → بتحجز مساحة في الذاكرة للكائن.
    
- `ClassName()` → استدعاء للـ constructor علشان تجهز الكائن.
  ### لما تكتب:

`Car c = new Car();`

بيحصل ٣ حاجات:

1. الnew Car() ← تنشئ كائن جديد من الكلاس `Car` في الـ **heap memory**
    
2. ال**`c`** ← متغير بيتخزن في الـ **stack memory**
    
3. الـ `c` بيشاور (يشير) على الكائن اللي اتخزن في الـ heap
   
   Student s1 = new Student(); // ← كائن بيتخزن في heap s1 = null; // ← السهم (reference) اتقطع
   - الكائن لسه موجود في الـ heap
    
- لكن **مافيش حاجة بتشاور عليه**
    

ودي بنسميها:

> 🔹 **Unreachable object**

---

## 💡 إمتى يتمسح بقى؟

Java عندها حاجة اسمها **Garbage Collector (GC)**  
دي وظيفتها تمسح أي object:

- **محدش بيشاور عليه**
    
- **ومش مستخدم**
    

بس دي مش بتشتغل فورًا، بتشتغل في الوقت اللي JVM تقرره.