# 💽 SQL Server Database Files

---

## 📁 أنواع ملفات قواعد البيانات:

كل قاعدة بيانات في SQL Server تتكون من ملفات تُخزَّن على الهارد ديسك. وهي:

---

## 1️⃣ .mdf – Primary Data File

- ✅ الملف الأساسي للبيانات.
- يحتوي على:
  - الجداول (Tables)
  - الفهارس (Indexes)
  - Views, Stored Procedures, إلخ.
- لازم يكون فيه ملف `.mdf` واحد على الأقل في كل قاعدة بيانات.

📌 **مثال**: `MyDB.mdf`

---

## 2️⃣ .ldf – Transaction Log File

- 🧾 يُسجل كل التعديلات اللي بتحصل على البيانات.
- يُستخدم في:
  - الاستعادة بعد الكراش.
  - تنفيذ عمليات `ROLLBACK` و `RECOVERY`.
- لا تحتوي على بيانات فعلية، بل تسجيل للعمليات.

📌 **مثال**: `MyDB_log.ldf`

---

## 3️⃣ .ndf – Secondary Data File

- ➕ ملفات بيانات إضافية (اختيارية).
- تُستخدم عندما:
  - القاعدة تكبر جدًا.
  - تحتاج لتوزيع البيانات على أكثر من قرص.
- ممكن يكون عندك أكثر من `.ndf`.

📌 **مثال**: `MyDB_Part2.ndf`

---

## 📊 مقارنة سريعة:

| نوع الملف  | الوظيفة                                      | أساسي؟ | الامتداد |
|------------|-----------------------------------------------|--------|-----------|
| `.mdf`     | يحتوي على البيانات الأساسية                   | ✅ نعم | `.mdf`    |
| `.ldf`     | يسجل العمليات والمعاملات (Logs)               | ✅ نعم | `.ldf`    |
| `.ndf`     | ملفات بيانات إضافية لتوسيع التخزين           | ❌ لا   | `.ndf`    |

---

## 🧠 مثال تطبيقي:

عند إنشاء قاعدة بيانات `SalesDB`، SQL Server ينشئ:

- `SalesDB.mdf` ← بيانات القاعدة
- `SalesDB_log.ldf` ← سجل العمليات

ولو أضفت ملف بيانات إضافي:
- `SalesDB_Data2.ndf`

---

## 🎯 أهمية معرفة أنواع الملفات:

- لتطبيق النسخ الاحتياطي والاسترجاع الصحيح.
- لفهم كيفية تخزين البيانات.
- لتحسين الأداء وتوزيع الحمل.
- لحل مشاكل الحجم والتخزين.

