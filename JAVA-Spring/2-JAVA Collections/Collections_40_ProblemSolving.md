# 🏆 40 مسألة Problem Solving على Java Collections
### من الصفر للاحتراف — بالعامية المصرية

---

> **الهدف:** إنك بعد ما تخلص الـ 40 مسألة دول، تبقى فاهم كل حاجة في Java Collections وتقدر تحل أي مشكلة تقابلك بطريقة منظمة ومرتبة.

> **إزاي تستخدم الملف ده:**
> 1. إقرأ المسألة كويس
> 2. فكّر في الحل لوحدك الأول (متبصش على خطوات الحل)
> 3. بعد كده قارن تفكيرك بخطوات الحل
> 4. اكتب الكود بنفسك
> 5. جرّب بالأمثلة الموجودة

---

## 📑 الفهرس

| المستوى | المسائل | المواضيع |
|---------|---------|----------|
| 🟢 مبتدئ | 1 - 12 | ArrayList, LinkedList, أساسيات List |
| 🟡 متوسط | 13 - 24 | HashSet, TreeSet, HashMap, TreeMap |
| 🔴 متقدم | 25 - 34 | Queue, PriorityQueue, Stack, Deque |
| ⚫ خبير | 35 - 40 | مسائل مركبة بتجمع أكتر من Collection |

---

---

# 🟢 المستوى الأول: مبتدئ (ArrayList & LinkedList)

---

## مسألة 1: إزالة التكرار من ليستة

**المسألة:**
عندك ArrayList فيها أرقام، بعض الأرقام متكررة. المطلوب إنك ترجّع ArrayList جديدة فيها نفس الأرقام بس من غير تكرار، وبنفس الترتيب الأصلي.

**المدخل:** `ArrayList<Integer>`
**المخرج:** `ArrayList<Integer>` من غير عناصر مكررة

**مثال 1:**
```
Input:  [4, 2, 7, 2, 4, 9, 7]
Output: [4, 2, 7, 9]
```

**مثال 2:**
```
Input:  [1, 1, 1, 1]
Output: [1]
```

**مثال 3:**
```
Input:  [5, 3, 8]
Output: [5, 3, 8]
```

**خطوات الحل (فكّر كده):**

1. **فهم المشكلة:** أنا محتاج أشيل التكرار بس أحافظ على ترتيب أول ظهور لكل عنصر.
2. **إيه اللي هيساعدني أعرف العنصر ده ظهر قبل كده ولا لأ؟** محتاج حاجة أخزن فيها العناصر اللي شفتها — يعني Set (لأنها بطبيعتها مبتقبلش تكرار).
3. **الخطة:** هعمل ArrayList جديدة فاضية و HashSet فاضية. هلف على الليستة الأصلية، كل عنصر هشوف لو مش موجود في الـ Set أضيفه في الـ Set وفي الـ ArrayList الجديدة. لو موجود أتخطاه.
4. **ليه HashSet مش TreeSet؟** لأني مش محتاج ترتيب، أنا بس محتاج أعرف موجود ولا لأ، و HashSet أسرع (O(1) للبحث).

---

## مسألة 2: إيجاد ثاني أكبر عنصر

**المسألة:**
عندك ArrayList فيها أرقام. المطلوب تلاقي ثاني أكبر رقم فيها.

**المدخل:** `ArrayList<Integer>`
**المخرج:** `int` (ثاني أكبر عنصر)

**مثال 1:**
```
Input:  [10, 5, 20, 8, 20]
Output: 10
```

**مثال 2:**
```
Input:  [3, 1, 4, 1, 5, 9]
Output: 5
```

**مثال 3:**
```
Input:  [7, 7, 7, 3]
Output: 3
```

**خطوات الحل (فكّر كده):**

1. **فهم المشكلة:** ثاني أكبر عنصر يعني أكبر عنصر بعد ما نشيل أكبر واحد (القيمة مش الموقع).
2. **طريقة بسيطة:** أقدر أعمل sort للـ ArrayList وآخد العنصر اللي قبل الأخير، بس لازم أاخد بالي من التكرار (لو آخر كذا عنصر نفس القيمة).
3. **طريقة أحسن:** ألف مرة واحدة بس وأحافظ على متغيرين: `max` و `secondMax`. كل عنصر جديد هشوف: لو أكبر من `max`، يبقى `secondMax = max` و `max = العنصر الجديد`. لو أصغر من `max` بس أكبر من `secondMax`، يبقى `secondMax = العنصر الجديد`.
4. **ليه الطريقة التانية أحسن؟** لأنها O(n) بدل O(n log n) للـ sorting.

---

## مسألة 3: عكس ArrayList

**المسألة:**
عندك ArrayList من Strings. المطلوب تعكس ترتيب العناصر (الأول يبقى الأخير والأخير يبقى الأول).

**المدخل:** `ArrayList<String>`
**المخرج:** `ArrayList<String>` معكوسة

**مثال 1:**
```
Input:  ["Ahmed", "Sara", "Ali"]
Output: ["Ali", "Sara", "Ahmed"]
```

**مثال 2:**
```
Input:  ["A", "B", "C", "D"]
Output: ["D", "C", "B", "A"]
```

**خطوات الحل (فكّر كده):**

1. **الطريقة السهلة:** `Collections.reverse()` — بس ده مش بيعلمك حاجة.
2. **فكّر يدوي:** استخدم two pointers — واحد من الأول وواحد من الآخر. بدّل العنصرين ببعض، وقرّب الاتنين لبعض لحد ما يتقابلوا في النص.
3. **يعني:** العنصر في index 0 يتبدل مع اللي في index (size-1)، وبعدين index 1 مع index (size-2)، وهكذا.
4. **امتى أوقف؟** لما الـ pointer الشمال يبقى >= الـ pointer اليمين.

---

## مسألة 4: دمج ليستتين مع الترتيب

**المسألة:**
عندك 2 ArrayList كل واحدة فيهم مرتبة من الأصغر للأكبر. المطلوب تدمجهم في ArrayList واحدة مرتبة.

**المدخل:** `ArrayList<Integer>` و `ArrayList<Integer>` (الاتنين مرتبين)
**المخرج:** `ArrayList<Integer>` مرتبة

**مثال 1:**
```
Input:  [1, 3, 5] و [2, 4, 6]
Output: [1, 2, 3, 4, 5, 6]
```

**مثال 2:**
```
Input:  [1, 1, 3] و [2, 2]
Output: [1, 1, 2, 2, 3]
```

**مثال 3:**
```
Input:  [10, 20] و [5, 15, 25]
Output: [5, 10, 15, 20, 25]
```

**خطوات الحل (فكّر كده):**

1. **الفكرة الأساسية (Merge):** عندي ليستتين مرتبين، هستخدم pointer على كل ليستة. أقارن العنصرين اللي عندي وأحط الأصغر في الليستة الجديدة وأمشي الـ pointer بتاعه خطوة.
2. **لو ليستة خلصت قبل التانية؟** أنقل الباقي كله من الليستة التانية على طول.
3. **ده اسمه Merge Sort technique** — نفس الفكرة بالظبط اللي بتحصل في خطوة الدمج في Merge Sort.
4. **ليه مجمعهمش في ليستة وأعمل sort؟** تقدر، بس الطريقة دي O(n+m) وهي الأسرع لأن الليستتين أصلاً مرتبين.

---

## مسألة 5: إيجاد العناصر المشتركة بين ليستتين

**المسألة:**
عندك 2 ArrayList. المطلوب ترجّع ArrayList فيها العناصر المشتركة بينهم (بدون تكرار).

**المدخل:** `ArrayList<Integer>` و `ArrayList<Integer>`
**المخرج:** `ArrayList<Integer>` فيها العناصر المشتركة

**مثال 1:**
```
Input:  [1, 2, 3, 4, 5] و [3, 4, 5, 6, 7]
Output: [3, 4, 5]
```

**مثال 2:**
```
Input:  [1, 2, 2, 3] و [2, 2, 4]
Output: [2]
```

**مثال 3:**
```
Input:  [10, 20] و [30, 40]
Output: []
```

**خطوات الحل (فكّر كده):**

1. **الفكرة:** محتاج أعرف كل عنصر في الليستة الأولى لو موجود في الليستة التانية.
2. **لو لفيت على كل عنصر في الأولى وبحثت عنه في التانية:** ده هيبقى O(n * m) — بطيء.
3. **الأحسن:** أحوّل الليستة التانية لـ HashSet. كده البحث فيها هيبقى O(1). ألف على الليستة الأولى وأشيك لو العنصر موجود في الـ Set.
4. **عشان أمنع التكرار في النتيجة:** أستخدم Set تاني للنتيجة، أو أشيل العنصر من الـ Set الأصلية بعد ما ألاقيه.

---

## مسألة 6: تقسيم ليستة لمجموعات

**المسألة:**
عندك ArrayList وعدد n. المطلوب تقسم الليستة لمجموعات كل مجموعة فيها n عناصر (آخر مجموعة ممكن تكون أقل).

**المدخل:** `ArrayList<String>` وعدد `int n`
**المخرج:** `ArrayList<ArrayList<String>>`

**مثال 1:**
```
Input:  ["A", "B", "C", "D", "E"] , n = 2
Output: [["A", "B"], ["C", "D"], ["E"]]
```

**مثال 2:**
```
Input:  [1, 2, 3, 4, 5, 6] , n = 3
Output: [[1, 2, 3], [4, 5, 6]]
```

**مثال 3:**
```
Input:  [1, 2, 3] , n = 5
Output: [[1, 2, 3]]
```

**خطوات الحل (فكّر كده):**

1. **الفكرة:** هلف على الليستة بخطوات حجمها n. يعني من 0 لـ n، وبعدين من n لـ 2n، وهكذا.
2. **في كل خطوة:** هاخد subList من index الحالي لـ index + n (أو لنهاية الليستة لو الباقي أقل من n).
3. **الأداة المناسبة:** `subList(fromIndex, toIndex)` بتاعت ArrayList أو ألف وأضيف يدوي.
4. **خلي بالك:** `Math.min(i + n, list.size())` عشان متعديش حدود الليستة.

---

## مسألة 7: تحريك الأصفار لآخر الليستة

**المسألة:**
عندك ArrayList فيها أرقام بعضها أصفار. المطلوب تحرك كل الأصفار لآخر الليستة مع الحفاظ على ترتيب باقي الأرقام.

**المدخل:** `ArrayList<Integer>`
**المخرج:** `ArrayList<Integer>` الأصفار في الآخر

**مثال 1:**
```
Input:  [0, 1, 0, 3, 12]
Output: [1, 3, 12, 0, 0]
```

**مثال 2:**
```
Input:  [0, 0, 1]
Output: [1, 0, 0]
```

**مثال 3:**
```
Input:  [1, 2, 3]
Output: [1, 2, 3]
```

**خطوات الحل (فكّر كده):**

1. **أول فكرة ممكن تيجي في بالك:** أشيل كل الأصفار وبعدين أضيفهم في الآخر — ده بيشتغل بس فيه عمليات remove كتير وهي غالية في ArrayList.
2. **فكرة أحسن — two pointers:** هخلي pointer اسمه `insertPos` بيشاور على أول مكان فاضي. ألف على الليستة: كل ما ألاقي عنصر مش صفر، أحطه في `insertPos` وأمشي `insertPos` خطوة.
3. **بعد ما أخلص اللفة:** كل الأماكن من `insertPos` لآخر الليستة أحطها أصفار.
4. **النتيجة:** لفيت مرة واحدة بس — O(n).

---

## مسألة 8: تدوير الليستة (Rotate)

**المسألة:**
عندك ArrayList وعدد k. المطلوب تدوّر الليستة k خطوات لليمين (آخر عنصر يروح الأول).

**المدخل:** `ArrayList<Integer>` وعدد `int k`
**المخرج:** `ArrayList<Integer>` بعد التدوير

**مثال 1:**
```
Input:  [1, 2, 3, 4, 5] , k = 2
Output: [4, 5, 1, 2, 3]
```

**مثال 2:**
```
Input:  [10, 20, 30] , k = 1
Output: [30, 10, 20]
```

**مثال 3:**
```
Input:  [1, 2, 3] , k = 3
Output: [1, 2, 3]
```

**خطوات الحل (فكّر كده):**

1. **أول حاجة:** لو k أكبر من حجم الليستة، يبقى k = k % size. لأن لو الليستة حجمها 5 وعملت 5 rotations هترجع زي ما هي.
2. **طريقة سهلة:** خد آخر k عناصر وحطهم في الأول. يعني اعمل subList من (size - k) للآخر + subList من الأول لـ (size - k).
3. **طريقة ذكية (3 reverses):** اعكس الليستة كلها، بعدين اعكس أول k عناصر، بعدين اعكس الباقي. هتلاقي النتيجة مظبوطة!
4. **ليه طريقة الـ 3 reverses حلوة؟** لأنها بتعمل كل حاجة in-place من غير ليستة جديدة.

---

## مسألة 9: إيجاد أول عنصر متكرر

**المسألة:**
عندك ArrayList. المطلوب تلاقي أول عنصر بيظهر أكتر من مرة (أول واحد في ترتيب الظهور بيتكرر لاحقاً).

**المدخل:** `ArrayList<Integer>`
**المخرج:** `int` (أول عنصر متكرر) أو -1 لو مفيش تكرار

**مثال 1:**
```
Input:  [5, 3, 4, 3, 5, 6]
Output: 5
```
(5 ظهر في index 0 وتكرر في index 4، و3 ظهر في index 1 وتكرر في index 3 — بس 5 ظهر الأول)

**مثال 2:**
```
Input:  [1, 2, 3, 2, 1]
Output: 1
```

**مثال 3:**
```
Input:  [1, 2, 3]
Output: -1
```

**خطوات الحل (فكّر كده):**

1. **فهم المسألة بدقة:** مش أول عنصر ألاقي تكرار له — ده أول عنصر في ترتيب ظهوره الأول بيتكرر بعدين.
2. **خطة:** أول حاجة أعرف مين اللي بيتكرر ومين لأ. أعمل HashMap تعد كل عنصر كام مرة ظهر.
3. **بعد كده:** ألف على الليستة الأصلية مرة تانية (مش الـ Map!) وأول عنصر ألاقي عدده أكتر من 1 هو ده الإجابة.
4. **ليه لفيت على الليستة مش الـ Map؟** لأن الـ HashMap مبتحافظش على ترتيب الإضافة (ممكن تستخدم LinkedHashMap لو عايز).

---

## مسألة 10: ترتيب ليستة من Strings حسب الطول

**المسألة:**
عندك ArrayList من Strings. المطلوب ترتبها حسب طول الـ String من الأقصر للأطول. لو عندك strings بنفس الطول، رتبهم أبجدياً.

**المدخل:** `ArrayList<String>`
**المخرج:** `ArrayList<String>` مرتبة حسب الطول

**مثال 1:**
```
Input:  ["banana", "kiwi", "apple", "fig"]
Output: ["fig", "kiwi", "apple", "banana"]
```

**مثال 2:**
```
Input:  ["cat", "bat", "ant"]
Output: ["ant", "bat", "cat"]
```

**مثال 3:**
```
Input:  ["hi", "a", "hey"]
Output: ["a", "hi", "hey"]
```

**خطوات الحل (فكّر كده):**

1. **محتاج أرتب بمعيار مخصوص:** مش الترتيب العادي (الأبجدي)، أنا عايز أرتب على حسب الطول.
2. **الأداة:** `Collections.sort()` مع Comparator مخصوص.
3. **الـ Comparator:** الأول أقارن بالطول `s1.length() - s2.length()`. لو متساويين في الطول أقارن أبجدياً `s1.compareTo(s2)`.
4. **ممكن كمان:** أستخدم `list.sort(Comparator.comparingInt(String::length).thenComparing(Comparator.naturalOrder()))` — أقصر وأنظف.

---

## مسألة 11: فلترة العناصر حسب شرط

**المسألة:**
عندك ArrayList من أرقام. المطلوب ترجّع ArrayList جديدة فيها بس الأرقام الزوجية اللي أكبر من 10.

**المدخل:** `ArrayList<Integer>`
**المخرج:** `ArrayList<Integer>` (الأرقام الزوجية > 10)

**مثال 1:**
```
Input:  [5, 12, 8, 20, 3, 14, 7]
Output: [12, 20, 14]
```

**مثال 2:**
```
Input:  [2, 4, 6, 8, 10]
Output: []
```

**مثال 3:**
```
Input:  [15, 22, 30, 11]
Output: [22, 30]
```

**خطوات الحل (فكّر كده):**

1. **المطلوب واضح:** ألف وأطبق شرطين: العدد زوجي (n % 2 == 0) وأكبر من 10 (n > 10).
2. **أعمل ليستة جديدة فاضية.** ألف على الأصلية، كل عنصر بيحقق الشرطين أضيفه.
3. **ممكن كمان تستخدم:** `removeIf()` على copy من الليستة: `copy.removeIf(n -> !(n % 2 == 0 && n > 10))`.
4. **أو بـ Stream:** `list.stream().filter(n -> n % 2 == 0 && n > 10).collect(Collectors.toList())`.
5. **الفكرة الأساسية:** لما تيجي تفلتر، فكّر في الشرط الإيجابي (إيه اللي عايزه)، مش السلبي.

---

## مسألة 12: LinkedList كـ Queue بسيطة

**المسألة:**
نفّذ نظام طابور بسيط لعيادة دكتور باستخدام LinkedList. لازم تدعم: إضافة مريض، خروج أول مريض، وعرض المريض التالي من غير ما يخرج.

**المدخل:** سلسلة من الأوامر: `ADD اسم` / `NEXT` / `PEEK`
**المخرج:** تنفيذ كل أمر وطباعة النتيجة

**مثال 1:**
```
Input:  ADD Ahmed, ADD Sara, PEEK, NEXT, PEEK
Output: PEEK -> Ahmed, NEXT -> Ahmed (removed), PEEK -> Sara
```

**مثال 2:**
```
Input:  ADD Ali, ADD Omar, NEXT, NEXT, PEEK
Output: NEXT -> Ali, NEXT -> Omar, PEEK -> Queue is empty
```

**خطوات الحل (فكّر كده):**

1. **إيه المطلوب؟** Queue — أول واحد يدخل أول واحد يخرج (FIFO).
2. **LinkedList بتعمل implement لـ Queue interface.** يعني تقدر تستخدم `offer()` للإضافة، `poll()` للشيل من الأول، `peek()` للبص من غير شيل.
3. **ليه LinkedList مش ArrayList؟** لأن الشيل من أول ArrayList غالي (O(n) — لازم يعمل shift لكل العناصر). بس LinkedList الشيل من الأول O(1).
4. **لازم أتعامل مع حالة الـ Queue فاضية:** قبل `poll()` أو `peek()` أشيك لو فاضية.

---

---

# 🟡 المستوى التاني: متوسط (Set & Map)

---

## مسألة 13: إيجاد أول حرف متفرد في String

**المسألة:**
عندك String. المطلوب تلاقي أول حرف فيها مش بيتكرر.

**المدخل:** `String`
**المخرج:** `char` (أول حرف مش متكرر) أو `'_'` لو كلهم متكررين

**مثال 1:**
```
Input:  "aabbcdd"
Output: 'c'
```

**مثال 2:**
```
Input:  "abcabc"
Output: '_'
```

**مثال 3:**
```
Input:  "hello"
Output: 'h'
```

**خطوات الحل (فكّر كده):**

1. **محتاج أعرف كل حرف ظهر كام مرة.** يعني HashMap<Character, Integer> للعد.
2. **ألف على الـ String الأول** وأملا الـ Map بعدد ظهور كل حرف.
3. **ألف مرة تانية على الـ String (مش الـ Map!):** وأول حرف ألاقي عدده 1 في الـ Map، هو ده الإجابة.
4. **ليه لفيت على الـ String مش الـ Map؟** لأن HashMap مبيحافظش على ترتيب الإدخال. لو عايز تلف على الـ Map بالترتيب استخدم LinkedHashMap.
5. **الدرس هنا:** لما تحتاج ترتيب + عد = LinkedHashMap هو صاحبك.

---

## مسألة 14: إيجاد التقاطع والفرق بين مجموعتين

**المسألة:**
عندك 2 HashSet. المطلوب تحسب: التقاطع (العناصر المشتركة)، الاتحاد (كل العناصر)، والفرق (عناصر في الأولى مش في التانية).

**المدخل:** `HashSet<String>` و `HashSet<String>`
**المخرج:** 3 Sets — التقاطع والاتحاد والفرق

**مثال 1:**
```
Input:  {Java, Python, C++} و {Python, JavaScript, Java}
Output:
  Intersection: {Java, Python}
  Union: {Java, Python, C++, JavaScript}
  Difference: {C++}
```

**مثال 2:**
```
Input:  {A, B} و {C, D}
Output:
  Intersection: {}
  Union: {A, B, C, D}
  Difference: {A, B}
```

**خطوات الحل (فكّر كده):**

1. **التقاطع:** أعمل copy من Set الأولى واستخدم `retainAll(set2)` — هيفضل بس العناصر الموجودة في الاتنين.
2. **الاتحاد:** أعمل copy من Set الأولى واستخدم `addAll(set2)` — هيضيف كل اللي في التانية (الـ Set بطبيعتها مش هتكرر).
3. **الفرق:** أعمل copy من Set الأولى واستخدم `removeAll(set2)` — هيشيل أي عنصر موجود في التانية.
4. **مهم جداً:** اعمل copy الأول! `new HashSet<>(set1)`. لو استخدمت الـ Set الأصلية هتتغير وتضيع عليك.

---

## مسألة 15: عد تكرار الكلمات

**المسألة:**
عندك String فيها جملة. المطلوب تعد كل كلمة ظهرت كام مرة وتطبع النتيجة.

**المدخل:** `String`
**المخرج:** `HashMap<String, Integer>`

**مثال 1:**
```
Input:  "java is great and java is fun"
Output: {java=2, is=2, great=1, and=1, fun=1}
```

**مثال 2:**
```
Input:  "hello hello hello"
Output: {hello=3}
```

**مثال 3:**
```
Input:  "one two three"
Output: {one=1, two=1, three=1}
```

**خطوات الحل (فكّر كده):**

1. **أول حاجة:** أقسم الجملة لكلمات باستخدام `split(" ")` — هيديني Array من Strings.
2. **أعمل HashMap<String, Integer>.** ألف على كل كلمة: لو موجودة في الـ Map أزوّد عددها بـ 1، لو مش موجودة أحطها بعدد 1.
3. **طريقة أنظف:** `map.getOrDefault(word, 0) + 1` — بدل ما أعمل if/else.
4. **طريقة أنظف كمان:** `map.merge(word, 1, Integer::sum)` — سطر واحد بس!
5. **لو عايز ترتيب أبجدي:** استخدم TreeMap بدل HashMap.

---

## مسألة 16: إيجاد العنصر الأكثر تكراراً

**المسألة:**
عندك ArrayList من أرقام. المطلوب تلاقي العنصر اللي بيتكرر أكتر حاجة. لو في أكتر من عنصر بنفس التكرار، رجّع أي واحد فيهم.

**المدخل:** `ArrayList<Integer>`
**المخرج:** `int`

**مثال 1:**
```
Input:  [1, 3, 2, 3, 1, 3, 2]
Output: 3
```

**مثال 2:**
```
Input:  [5, 5, 5, 1, 1]
Output: 5
```

**مثال 3:**
```
Input:  [7]
Output: 7
```

**خطوات الحل (فكّر كده):**

1. **الخطوة الأولى:** أعد تكرار كل عنصر — HashMap<Integer, Integer>.
2. **الخطوة التانية:** ألف على entries بتاعت الـ Map وألاقي أكبر value.
3. **إزاي ألاقي أكبر value؟** أحافظ على متغيرين: `maxCount` و `maxElement`. كل entry ألاقي الـ value بتاعتها أكبر من `maxCount`، أحدّث الاتنين.
4. **ممكن كمان:** `Collections.max(map.entrySet(), Map.Entry.comparingByValue()).getKey()` — سطر واحد.
5. **الباترن ده (عد + إيجاد الأكبر) هيتكرر معاك كتير.** احفظه كويس!

---

## مسألة 17: Two Sum

**المسألة:**
عندك ArrayList من أرقام وعدد target. المطلوب تلاقي أي عنصرين مجموعهم يساوي الـ target وترجّع الـ indices بتوعهم.

**المدخل:** `ArrayList<Integer>` و `int target`
**المخرج:** `int[]` فيه 2 indices (أو [-1, -1] لو مفيش حل)

**مثال 1:**
```
Input:  [2, 7, 11, 15] , target = 9
Output: [0, 1]  (لأن 2 + 7 = 9)
```

**مثال 2:**
```
Input:  [3, 2, 4] , target = 6
Output: [1, 2]  (لأن 2 + 4 = 6)
```

**مثال 3:**
```
Input:  [1, 2, 3] , target = 10
Output: [-1, -1]
```

**خطوات الحل (فكّر كده):**

1. **الطريقة البسيطة (Brute Force):** جرّب كل زوج ممكن — لفّتين nested. ده O(n²) — بطيء.
2. **الطريقة الذكية (HashMap):** لكل عنصر، أنا عارف إن المطلوب يكون معاه عنصر تاني = target - العنصر. يعني لو العنصر 2 والـ target 9، أنا بدور على 7.
3. **الخطة:** أعمل HashMap<Integer, Integer> — الـ key هي القيمة والـ value هي الـ index. ألف على الليستة: لكل عنصر أشوف لو (target - العنصر) موجود في الـ Map. لو أيوه لقيت الإجابة. لو لأ أضيف العنصر الحالي في الـ Map.
4. **ده من أشهر المسائل في الـ interviews!** الباترن ده اسمه "Complement Search" وبيتكرر كتير.

---

## مسألة 18: تجميع العناصر (Group By)

**المسألة:**
عندك ArrayList من Strings. المطلوب تجمّعهم حسب أول حرف. يعني كل الكلمات اللي بتبدأ بنفس الحرف تبقى مع بعض.

**المدخل:** `ArrayList<String>`
**المخرج:** `HashMap<Character, ArrayList<String>>`

**مثال 1:**
```
Input:  ["apple", "banana", "avocado", "blueberry", "cherry", "apricot"]
Output: {a=[apple, avocado, apricot], b=[banana, blueberry], c=[cherry]}
```

**مثال 2:**
```
Input:  ["dog", "deer", "cat"]
Output: {d=[dog, deer], c=[cat]}
```

**خطوات الحل (فكّر كده):**

1. **محتاج Map اللي الـ key فيها هي الحرف الأول والـ value هي ليستة من الكلمات.**
2. **ألف على الكلمات:** كل كلمة آخد أول حرف `word.charAt(0)`.
3. **أشوف لو الحرف ده موجود كـ key في الـ Map:** لو أيوه أضيف الكلمة على الليستة الموجودة. لو لأ أعمل ليستة جديدة وأحطها.
4. **طريقة أنظف:** `map.computeIfAbsent(firstChar, k -> new ArrayList<>()).add(word)` — سطر واحد بيعمل كل حاجة!
5. **الباترن ده (Grouping) مهم جداً** وبتستخدمه كتير في الشغل الحقيقي.

---

## مسألة 19: التحقق من الـ Anagram

**المسألة:**
عندك 2 Strings. المطلوب تعرف لو هم anagram (بيتكونوا من نفس الحروف بنفس العدد بس بترتيب مختلف).

**المدخل:** `String` و `String`
**المخرج:** `boolean`

**مثال 1:**
```
Input:  "listen" و "silent"
Output: true
```

**مثال 2:**
```
Input:  "hello" و "world"
Output: false
```

**مثال 3:**
```
Input:  "aab" و "aba"
Output: true
```

**خطوات الحل (فكّر كده):**

1. **أول تشيك:** لو الطول مختلف، مش anagram — ارجع false فوراً.
2. **طريقة 1 (Sorting):** حوّل كل String لـ char array، اعمل sort، وقارن. لو متساويين يبقى anagram. ده O(n log n).
3. **طريقة 2 (HashMap — أحسن):** أعمل HashMap<Character, Integer>. ألف على الـ String الأولى وأزوّد عدد كل حرف. ألف على التانية وأنقّص عدد كل حرف. لو في الآخر كل القيم 0 يبقى anagram.
4. **طريقة 3 (أبسط باستخدام Map واحدة):** زوّد مع الأولى وقلّل مع التانية. لو أي قيمة بقت سالبة أو في الآخر مش كلها صفر — مش anagram.

---

## مسألة 20: تحويل ليستة لـ Map

**المسألة:**
عندك ArrayList من الطلاب (كل طالب عبارة عن String بالشكل "اسم:درجة"). المطلوب تحوّلها لـ HashMap اللي الـ key فيها الاسم والـ value هي الدرجة.

**المدخل:** `ArrayList<String>`
**المخرج:** `HashMap<String, Integer>`

**مثال 1:**
```
Input:  ["Ahmed:90", "Sara:85", "Ali:95"]
Output: {Ahmed=90, Sara=85, Ali=95}
```

**مثال 2:**
```
Input:  ["A:100", "B:50"]
Output: {A=100, B=50}
```

**مثال 3:**
```
Input:  ["Ahmed:90", "Ahmed:95"]
Output: {Ahmed=95}  (آخر قيمة بتكسب)
```

**خطوات الحل (فكّر كده):**

1. **ألف على الليستة:** كل String أقسمه بـ `split(":")`.
2. **الجزء الأول** هو الاسم (key) والجزء التاني هو الدرجة — بس محتاج أحوله لـ Integer بـ `Integer.parseInt()`.
3. **أحط في الـ Map:** `map.put(name, grade)`.
4. **خلي بالك:** لو نفس الاسم اتكرر، الـ `put` هيعمل overwrite لآخر قيمة. لو عايز تتحكم (مثلاً تحتفظ بالأعلى)، استخدم `map.merge(name, grade, Math::max)`.

---

## مسألة 21: ترتيب Map حسب القيم

**المسألة:**
عندك HashMap<String, Integer> فيها أسماء ودرجات. المطلوب تطبع الأسماء مرتبة حسب الدرجة من الأعلى للأقل.

**المدخل:** `HashMap<String, Integer>`
**المخرج:** `List<String>` مرتبة حسب الدرجة تنازلياً

**مثال 1:**
```
Input:  {Ahmed=85, Sara=95, Ali=90}
Output: [Sara, Ali, Ahmed]
```

**مثال 2:**
```
Input:  {A=50, B=50, C=100}
Output: [C, A, B]  (أو [C, B, A] — A و B بنفس الدرجة)
```

**خطوات الحل (فكّر كده):**

1. **المشكلة:** HashMap مش مرتبة. محتاج أرتب الـ entries حسب الـ value.
2. **الخطة:** أجيب الـ entries كلها كـ List، وأعملها sort باستخدام Comparator بيقارن على الـ value.
3. **يعني:** `new ArrayList<>(map.entrySet())` وبعدين `list.sort((e1, e2) -> e2.getValue() - e1.getValue())` — لاحظ e2 - e1 مش e1 - e2 عشان تنازلي.
4. **بعد الترتيب:** ألف على الليستة المرتبة وآخد الـ keys.
5. **ممكن كمان:** TreeMap مش هتنفع هنا لأنها بترتب حسب الـ key مش الـ value.

---

## مسألة 22: إيجاد الأزواج المتكاملة

**المسألة:**
عندك ArrayList من أرقام وعدد target. المطلوب تلاقي كل الأزواج اللي مجموعهم = target (بدون تكرار أزواج).

**المدخل:** `ArrayList<Integer>` و `int target`
**المخرج:** `HashSet<String>` من الأزواج

**مثال 1:**
```
Input:  [1, 2, 3, 4, 5, 6] , target = 7
Output: {(1,6), (2,5), (3,4)}
```

**مثال 2:**
```
Input:  [1, 1, 2, 3] , target = 4
Output: {(1,3)}
```

**مثال 3:**
```
Input:  [5, 5, 5] , target = 10
Output: {(5,5)}
```

**خطوات الحل (فكّر كده):**

1. **شبه Two Sum بس المطلوب كل الأزواج مش أول زوج بس.**
2. **أستخدم HashSet للعناصر اللي شفتها.** ألف على كل عنصر: المطلوب complement = target - العنصر. لو الـ complement موجود في الـ Set — ده زوج!
3. **عشان أمنع التكرار في الأزواج:** أحط الزوج بترتيب (الأصغر, الأكبر) في Set من النتائج. كده لو لقيت (3,4) مرة و(4,3) مرة، هيتخزنوا مرة واحدة بس.
4. **في الآخر:** أضيف العنصر الحالي للـ Set.

---

## مسألة 23: التحقق من الـ Subset

**المسألة:**
عندك 2 HashSets. المطلوب تعرف لو الأولى subset من التانية (كل عناصر الأولى موجودة في التانية).

**المدخل:** `HashSet<Integer>` و `HashSet<Integer>`
**المخرج:** `boolean`

**مثال 1:**
```
Input:  {1, 2} و {1, 2, 3, 4}
Output: true
```

**مثال 2:**
```
Input:  {1, 5} و {1, 2, 3}
Output: false
```

**مثال 3:**
```
Input:  {} و {1, 2}
Output: true  (المجموعة الفاضية subset من أي مجموعة)
```

**خطوات الحل (فكّر كده):**

1. **الطريقة السهلة:** `set2.containsAll(set1)` — سطر واحد بس! بس خلينا نفهم إيه اللي بيحصل جوه.
2. **يدوياً:** ألف على كل عنصر في set1 وأشيك لو موجود في set2. لو لقيت عنصر واحد مش موجود — ارجع false.
3. **لو كلهم لقيتهم:** ارجع true.
4. **Complexity:** O(n) حيث n هو حجم set1 — لأن البحث في HashSet هو O(1).

---

## مسألة 24: تحويل TreeMap لترتيب مخصوص

**المسألة:**
عندك HashMap<String, List<String>> بتمثل الطلاب ومواد كل طالب. المطلوب تعرض الطلاب مرتبين أبجدياً وكل طالب مواده مرتبة.

**المدخل:** `HashMap<String, List<String>>`
**المخرج:** عرض منظم ومرتب

**مثال 1:**
```
Input:  {Sara=[Math, Art], Ahmed=[Science, Math, Art], Ali=[Math]}
Output:
  Ahmed: [Art, Math, Science]
  Ali: [Math]
  Sara: [Art, Math]
```

**مثال 2:**
```
Input:  {B=[Z, A], A=[C, B]}
Output:
  A: [B, C]
  B: [A, Z]
```

**خطوات الحل (فكّر كده):**

1. **عشان الأسماء تبقى مرتبة:** أحوّل الـ HashMap لـ TreeMap. الـ TreeMap بطبيعتها بترتب الـ keys.
2. **يعني:** `TreeMap<String, List<String>> sorted = new TreeMap<>(originalMap)`.
3. **عشان المواد تبقى مرتبة:** ألف على كل entry وأعمل sort للـ List بتاعتها — `Collections.sort(entry.getValue())`.
4. **الدرس هنا:** TreeMap للترتيب حسب الـ keys. لو عايز ترتيب حسب الـ values لازم تلف بنفسك أو تستخدم Stream.

---

---

# 🔴 المستوى التالت: متقدم (Queue, Stack, PriorityQueue, Deque)

---

## مسألة 25: التحقق من الأقواس المتوازنة

**المسألة:**
عندك String فيها أقواس `()`, `{}`, `[]`. المطلوب تعرف لو الأقواس متوازنة (كل قوس مفتوح ليه قوس مقفول مناسب وبالترتيب الصح).

**المدخل:** `String`
**المخرج:** `boolean`

**مثال 1:**
```
Input:  "({[]})"
Output: true
```

**مثال 2:**
```
Input:  "({[})"
Output: false
```

**مثال 3:**
```
Input:  "((()))"
Output: true
```

**خطوات الحل (فكّر كده):**

1. **ده Classic Stack Problem!** كل ما ألاقي قوس مفتوح أحطه في الـ Stack.
2. **كل ما ألاقي قوس مقفول:** أشيك لو أعلى عنصر في الـ Stack هو القوس المفتوح المناظر. لو أيوه أعمل pop. لو لأ — مش متوازن.
3. **في الآخر:** لو الـ Stack فاضي يبقى متوازن. لو لسه فيه حاجة يبقى مش متوازن.
4. **الحالات الخاصة:** String فاضي — متوازن. بيبدأ بقوس مقفول — مش متوازن (الـ Stack هيبقى فاضي لما أحاول أعمل pop).
5. **ممكن تستخدم:** `Deque<Character> stack = new ArrayDeque<>()` بدل Stack القديمة.

---

## مسألة 26: تنفيذ Stack باستخدام Queue

**المسألة:**
نفّذ Stack (LIFO) باستخدام Queue واحدة أو اتنين بس (FIFO). يعني push و pop و top.

**المدخل:** سلسلة أوامر
**المخرج:** تنفيذ الأوامر

**مثال 1:**
```
Input:  push(1), push(2), push(3), pop(), top()
Output: pop -> 3, top -> 2
```

**مثال 2:**
```
Input:  push(10), push(20), pop(), pop()
Output: pop -> 20, pop -> 10
```

**خطوات الحل (فكّر كده):**

1. **المشكلة:** Queue بتشيل من الأول (FIFO)، بس أنا عايز أشيل من الآخر (LIFO).
2. **الفكرة (Queue واحدة):** لما أعمل push، أضيف العنصر الجديد في الآخر. بعدين أعمل dequeue وenqueue لكل العناصر القديمة — كده العنصر الجديد يبقى في الأول!
3. **يعني:** لو الـ Queue فيها [1, 2] وعملت push(3): أضيف 3 فتبقى [1, 2, 3]. بعدين أشيل وأرجع 1 و 2: [3, 1, 2]. كده pop هيشيل 3 وهو آخر واحد دخل.
4. **الـ pop و top:** عاديين زي الـ Queue — شيل/بص على الأول.
5. **الدرس:** فهمك لطبيعة كل Data Structure بيخليك تحوّل بينهم.

---

## مسألة 27: أقرب عنصر أصغر

**المسألة:**
عندك ArrayList من أرقام. لكل عنصر، لاقي أقرب عنصر أصغر منه على اليسار. لو مفيش اكتب -1.

**المدخل:** `ArrayList<Integer>`
**المخرج:** `ArrayList<Integer>`

**مثال 1:**
```
Input:  [4, 5, 2, 10, 8]
Output: [-1, 4, -1, 2, 2]
```

**مثال 2:**
```
Input:  [3, 2, 1]
Output: [-1, -1, -1]
```

**مثال 3:**
```
Input:  [1, 3, 5, 7]
Output: [-1, 1, 3, 5]
```

**خطوات الحل (فكّر كده):**

1. **طريقة Brute Force:** لكل عنصر، لف على كل اللي قبله من اليمين لليسار ولاقي أول واحد أصغر. ده O(n²).
2. **طريقة Stack (أحسن — O(n)):** أستخدم Stack. الفكرة إني بحافظ على Stack فيها العناصر بترتيب تصاعدي من تحت لفوق.
3. **لكل عنصر جديد:** أعمل pop لكل اللي في الـ Stack أكبر منه أو يساويه (مش هيحتاجهم حد بعد كده). اللي هيفضل على الـ top هو أقرب عنصر أصغر. لو الـ Stack فاضي يبقى -1.
4. **بعدين أحط العنصر الحالي في الـ Stack.**
5. **ليه ده بيشتغل؟** لأن الـ Stack بتحافظ على "المرشحين المحتملين" بترتيب — لو عنصر أكبر من الحالي مش هيبقى أقرب أصغر لأي عنصر بعد كده.

---

## مسألة 28: PriorityQueue — أكبر k عناصر

**المسألة:**
عندك ArrayList كبيرة من أرقام وعدد k. المطلوب تلاقي أكبر k أرقام فيها.

**المدخل:** `ArrayList<Integer>` و `int k`
**المخرج:** `ArrayList<Integer>` (أكبر k عناصر)

**مثال 1:**
```
Input:  [3, 1, 4, 1, 5, 9, 2, 6] , k = 3
Output: [9, 6, 5]  (أو بأي ترتيب)
```

**مثال 2:**
```
Input:  [10, 20, 30, 40, 50] , k = 2
Output: [50, 40]
```

**مثال 3:**
```
Input:  [7, 7, 7, 7] , k = 2
Output: [7, 7]
```

**خطوات الحل (فكّر كده):**

1. **طريقة بسيطة:** Sort الليستة وخد آخر k عناصر. ده O(n log n).
2. **طريقة أحسن — Min Heap (PriorityQueue):** أعمل PriorityQueue حجمها k. ده Min Heap يعني أصغر عنصر على الـ top.
3. **الخطة:** ألف على الأرقام. كل رقم جديد: لو الـ Queue لسه مش full (حجمها < k) — أضيفه. لو full وهو أكبر من الـ top (أصغر عنصر في الـ Queue) — أشيل الـ top وأحط الرقم الجديد.
4. **في الآخر:** الـ Queue فيها أكبر k أرقام!
5. **ليه Min Heap مش Max Heap؟** لأني عايز أقدر أشيل أصغر عنصر بسهولة عشان لو لقيت أكبر منه أبدله.
6. **Complexity:** O(n log k) — أحسن من O(n log n) لما k صغير.

---

## مسألة 29: نظام المهام بالأولوية

**المسألة:**
صمم نظام بيدير مهام (Tasks) كل مهمة ليها اسم وأولوية (رقم أقل = أولوية أعلى). المطلوب إضافة مهام وتنفيذ المهمة الأعلى أولوية الأول.

**المدخل:** سلسلة أوامر `ADD اسم أولوية` / `EXECUTE`
**المخرج:** تنفيذ الأوامر

**مثال 1:**
```
Input:  ADD Bug_Fix 1, ADD Feature 3, ADD Test 2, EXECUTE, EXECUTE
Output: EXECUTE -> Bug_Fix (priority 1), EXECUTE -> Test (priority 2)
```

**مثال 2:**
```
Input:  ADD A 5, ADD B 5, EXECUTE, EXECUTE
Output: EXECUTE -> A (priority 5), EXECUTE -> B (priority 5)
```

**خطوات الحل (فكّر كده):**

1. **المطلوب:** كل مرة أنفذ، أجيب المهمة الأعلى أولوية — يعني PriorityQueue.
2. **المهمة:** أعمل class Task فيه name و priority.
3. **الـ PriorityQueue محتاجة Comparator:** بيقارن على الـ priority. `Comparator.comparingInt(Task::getPriority)`.
4. **ADD:** أعمل `queue.offer(new Task(name, priority))`.
5. **EXECUTE:** أعمل `queue.poll()` — هيديني المهمة بأقل priority number (أعلى أولوية).
6. **لو أولويات متساوية:** ممكن تضيف شرط تاني في الـ Comparator (مثلاً بالترتيب الأبجدي أو بترتيب الوصول).

---

## مسألة 30: تنفيذ LRU Cache بسيط

**المسألة:**
نفّذ Cache بيشتغل بنظام LRU (Least Recently Used). الـ Cache ليه سعة محددة. لو امتلأ وعايز أضيف عنصر جديد، أشيل العنصر اللي أقدم ما اتستخدم.

**المدخل:** سلسلة أوامر `PUT key value` / `GET key` مع سعة محددة
**المخرج:** نتيجة كل GET

**مثال 1:**
```
Capacity = 2
PUT A 1, PUT B 2, GET A, PUT C 3, GET B
Output: GET A -> 1, GET B -> -1
(B اتشال عشان C دخل والأقل استخداماً كان B لأن A اتستخدم في GET)
```

**مثال 2:**
```
Capacity = 1
PUT X 10, PUT Y 20, GET X
Output: GET X -> -1  (X اتشال لما Y دخل)
```

**خطوات الحل (فكّر كده):**

1. **المشكلة:** محتاج أعرف آخر مرة كل عنصر اتستخدم، وأشيل الأقدم.
2. **الحل المثالي:** LinkedHashMap! هو HashMap بيحافظ على ترتيب الإدخال (أو ترتيب الوصول لو فعّلت access-order).
3. **`new LinkedHashMap<>(capacity, 0.75f, true)`:** الـ `true` ده معناه access-order — كل ما تعمل get لعنصر بيروح آخر واحد.
4. **override الـ `removeEldestEntry`:** ترجع `true` لو الحجم > السعة — كده هيشيل أقدم عنصر تلقائياً.
5. **ممكن كمان تنفذه يدوي:** باستخدام HashMap + Doubly Linked List — بس LinkedHashMap جاهزة وبتعمل نفس الشغل.

---

## مسألة 31: Sliding Window Maximum

**المسألة:**
عندك ArrayList من أرقام و window size k. المطلوب لكل نافذة (window) بحجم k تلاقي أكبر رقم فيها.

**المدخل:** `ArrayList<Integer>` و `int k`
**المخرج:** `ArrayList<Integer>`

**مثال 1:**
```
Input:  [1, 3, -1, -3, 5, 3, 6, 7] , k = 3
Output: [3, 3, 5, 5, 6, 7]
```
(النوافذ: [1,3,-1]=3, [3,-1,-3]=3, [-1,-3,5]=5, [-3,5,3]=5, [5,3,6]=6, [3,6,7]=7)

**مثال 2:**
```
Input:  [1, 2, 3, 4] , k = 2
Output: [2, 3, 4]
```

**خطوات الحل (فكّر كده):**

1. **Brute Force:** لكل نافذة ألف على الـ k عناصر وألاقي الأكبر. O(n*k).
2. **أحسن — Deque:** أستخدم ArrayDeque (أو LinkedList كـ Deque). الفكرة إني أحافظ على الـ indices في الـ Deque بحيث القيم بتاعتها بتنزل من الأول (front) للآخر (back).
3. **لكل عنصر جديد:** أشيل من آخر الـ Deque أي index قيمته أصغر من العنصر الجديد (مش هنحتاجهم). أضيف index العنصر الجديد في الآخر.
4. **أشيل من أول الـ Deque** لو الـ index خرج من النافذة الحالية (index <= i - k).
5. **أول الـ Deque دايماً** هو index أكبر عنصر في النافذة.
6. **Complexity:** O(n) — كل عنصر بيدخل ويخرج من الـ Deque مرة واحدة بس.

---

## مسألة 32: تقييم تعبيرات حسابية (Postfix)

**المسألة:**
عندك تعبير حسابي بصيغة Postfix (الأرقام الأول وبعدين العملية). المطلوب تحسب النتيجة.

**المدخل:** `ArrayList<String>` (كل عنصر إما رقم أو عملية +, -, *, /)
**المخرج:** `int`

**مثال 1:**
```
Input:  ["2", "3", "+"]
Output: 5  (2 + 3)
```

**مثال 2:**
```
Input:  ["4", "13", "5", "/", "+"]
Output: 6  (13/5=2, 4+2=6)
```

**مثال 3:**
```
Input:  ["2", "1", "+", "3", "*"]
Output: 9  ((2+1)*3)
```

**خطوات الحل (فكّر كده):**

1. **Postfix = Stack Problem!** القاعدة: لو رقم — حطه في الـ Stack. لو عملية — اسحب آخر رقمين من الـ Stack، اعمل العملية، وحط النتيجة في الـ Stack.
2. **ألف على العناصر:** كل عنصر أشوف لو رقم ولا عملية.
3. **لو عملية:** `b = stack.pop()`, `a = stack.pop()`, النتيجة = a عملية b. حط النتيجة في الـ Stack.
4. **خلي بالك:** الترتيب مهم! `a` اللي اتسحبت الأول هي اللي اتحطت قبل — يعني هي اللي على اليسار.
5. **في الآخر:** الـ Stack هيبقى فيه رقم واحد — ده النتيجة.

---

## مسألة 33: تنفيذ Undo/Redo

**المسألة:**
نفّذ نظام Undo/Redo لمحرر نصوص بسيط. المطلوب دعم: كتابة نص (TYPE)، تراجع (UNDO)، إعادة (REDO)، وعرض النص الحالي (SHOW).

**المدخل:** سلسلة أوامر
**المخرج:** النص بعد كل SHOW

**مثال 1:**
```
Input:  TYPE "Hello ", TYPE "World", UNDO, SHOW, REDO, SHOW
Output: SHOW -> "Hello ", SHOW -> "Hello World"
```

**مثال 2:**
```
Input:  TYPE "A", TYPE "B", TYPE "C", UNDO, UNDO, SHOW
Output: SHOW -> "A"
```

**خطوات الحل (فكّر كده):**

1. **المطلوب:** أحافظ على تاريخ التغييرات عشان أقدر أرجع وأقدّم.
2. **هستخدم 2 Stacks:** واحدة اسمها `undoStack` والتانية `redoStack`.
3. **TYPE:** أضيف النص الحالي (قبل التغيير) على الـ undoStack، أعمل التغيير، وأفضّي الـ redoStack (لأن أي REDO قديم مالوش لازمة بعد كتابة جديدة).
4. **UNDO:** أنقل الحالة الحالية للـ redoStack، وأرجع الحالة اللي فوق الـ undoStack.
5. **REDO:** أنقل الحالة الحالية للـ undoStack، وأرجع الحالة اللي فوق الـ redoStack.
6. **الدرس:** 2 Stacks هي الطريقة الكلاسيكية لتنفيذ Undo/Redo.

---

## مسألة 34: BFS باستخدام Queue

**المسألة:**
عندك Graph ممثل بـ HashMap<String, List<String>> (Adjacency List). المطلوب تعمل BFS (Breadth-First Search) من نقطة بداية وتطبع الترتيب.

**المدخل:** `HashMap<String, List<String>>` و String البداية
**المخرج:** `ArrayList<String>` ترتيب الزيارة

**مثال 1:**
```
Input:  {A=[B,C], B=[D], C=[D,E], D=[], E=[]}  بداية = A
Output: [A, B, C, D, E]
```

**مثال 2:**
```
Input:  {1=[2,3], 2=[4], 3=[4], 4=[]}  بداية = 1
Output: [1, 2, 3, 4]
```

**خطوات الحل (فكّر كده):**

1. **BFS = Queue + Set!** الـ Queue عشان الترتيب (الأقرب الأول) والـ Set عشان منزورش نفس النقطة مرتين.
2. **البداية:** حط نقطة البداية في الـ Queue وفي الـ Visited Set.
3. **اللفة:** طالما الـ Queue مش فاضية: اسحب عنصر (poll)، ضيفه للنتيجة، وبعدين بص على كل جيرانه — لو جار مش في الـ Visited أضيفه في الـ Queue والـ Visited.
4. **ليه Queue؟** لأنها FIFO — هتزور المستوى كله قبل ما تنزل للمستوى اللي بعده.
5. **ليه Visited Set؟** عشان الـ Graph ممكن يكون فيه cycles — من غير الـ Set هتفضل تلف في حلقة لا نهائية.

---

---

# ⚫ المستوى الرابع: خبير (مسائل مركبة)

---

## مسألة 35: نظام إدارة المخزون

**المسألة:**
صمم نظام مخزون لمحل. كل منتج ليه اسم وكمية وسعر. المطلوب:
1. إضافة/تحديث منتج
2. البحث عن منتج
3. عرض المنتجات مرتبة بالسعر
4. عرض المنتجات اللي كميتها أقل من حد معين (Low Stock)

**المدخل:** أوامر مختلفة
**المخرج:** نتائج كل أمر

**مثال 1:**
```
ADD Laptop 10 5000
ADD Phone 25 3000
ADD Tablet 3 4000
LOW_STOCK 5
SORT_BY_PRICE
```
```
Output:
LOW_STOCK -> [Tablet(3)]
SORT_BY_PRICE -> [Phone(3000), Tablet(4000), Laptop(5000)]
```

**خطوات الحل (فكّر كده):**

1. **أول حاجة:** أعمل class Product فيه name, quantity, price.
2. **التخزين الأساسي:** HashMap<String, Product> — عشان البحث بالاسم يكون O(1).
3. **عرض مرتبة بالسعر:** آخد كل الـ values من الـ Map، أحولها لـ ArrayList، وأعمل sort باستخدام Comparator على السعر.
4. **Low Stock:** ألف على الـ values وأفلتر اللي الكمية بتاعتها أقل من الحد.
5. **لو عايز ترتيب دائم بالسعر:** ممكن تستخدم TreeMap بـ Comparator مخصوص، أو تحافظ على TreeSet جنب الـ HashMap.
6. **الدرس:** في المشاكل الكبيرة، ممكن تحتاج أكتر من Collection واحدة — كل واحدة بتخدم غرض معين.

---

## مسألة 36: تحليل سجل المواقع (Log Analysis)

**المسألة:**
عندك ArrayList من log entries كل واحد بالشكل "timestamp user action page". المطلوب:
1. إيه أكتر صفحة اتزارت؟
2. إيه أكتر user نشاطاً؟
3. اعرض كل user والصفحات اللي زارها (بدون تكرار)

**المدخل:** `ArrayList<String>`
**المخرج:** إجابات الأسئلة التلاتة

**مثال 1:**
```
Input:
  "10:00 Ahmed visit Home"
  "10:01 Sara visit Products"
  "10:02 Ahmed visit Products"
  "10:03 Ahmed visit Home"
  "10:04 Sara visit Home"

Output:
  Most visited page: Home (3 visits)
  Most active user: Ahmed (3 actions)
  Ahmed -> [Home, Products]
  Sara -> [Home, Products]
```

**خطوات الحل (فكّر كده):**

1. **Parse الأول:** كل entry أقسمه بـ split. أطلع منه الـ user و page.
2. **أكتر صفحة:** HashMap<String, Integer> بتعد visits لكل صفحة. بعدين ألاقي أكبر value.
3. **أكتر user نشاطاً:** HashMap<String, Integer> تاني بيعد actions لكل user. نفس الفكرة.
4. **كل user وصفحاته:** HashMap<String, HashSet<String>> — الـ key هو الـ user والـ value هي Set من الصفحات (Set عشان بدون تكرار).
5. **ممكن تعمل كل ده في لفة واحدة!** ألف مرة واحدة على الـ logs وأملا الـ 3 Maps في نفس الوقت.
6. **الدرس:** مش لازم تلف كذا مرة — حاول تجمع المعلومات في لفة واحدة لما يكون ممكن.

---

## مسألة 37: Merge Intervals

**المسألة:**
عندك ArrayList من فترات زمنية (intervals) كل واحدة بداية ونهاية. المطلوب تدمج الفترات المتداخلة.

**المدخل:** `ArrayList<int[]>` (كل int[] فيه [start, end])
**المخرج:** `ArrayList<int[]>` بعد الدمج

**مثال 1:**
```
Input:  [[1,3], [2,6], [8,10], [15,18]]
Output: [[1,6], [8,10], [15,18]]
```

**مثال 2:**
```
Input:  [[1,4], [4,5]]
Output: [[1,5]]
```

**مثال 3:**
```
Input:  [[1,4], [2,3]]
Output: [[1,4]]
```

**خطوات الحل (فكّر كده):**

1. **أول خطوة مهمة:** Sort الفترات حسب البداية. استخدم `list.sort(Comparator.comparingInt(a -> a[0]))`.
2. **أعمل ليستة نتيجة (أو Stack):** حط أول فترة.
3. **ألف من التانية:** كل فترة جديدة أقارنها بآخر فترة في النتيجة. لو بداية الجديدة <= نهاية الأخيرة — فيه تداخل! أدمجهم (نهاية الفترة المدمجة = أكبر نهاية بينهم).
4. **لو مفيش تداخل:** أضيف الفترة الجديدة في النتيجة.
5. **ليه Sort الأول؟** لأن بعد الترتيب، الفترات المتداخلة هتبقى جنب بعض — مش محتاج أقارن كل فترة بكل الفترات.
6. **Stack أو ArrayList؟** الاتنين هيشتغلوا — المهم أقدر أوصل لآخر عنصر بسهولة.

---

## مسألة 38: تصميم نظام Leaderboard

**المسألة:**
صمم Leaderboard للاعبين. كل لاعب ليه اسم ونقاط. المطلوب:
1. إضافة/تحديث نقاط لاعب
2. عرض أعلى k لاعبين
3. ترتيب لاعب معين (يعني هو رقم كام)
4. إزالة لاعب

**المدخل:** أوامر مختلفة
**المخرج:** نتائج كل أمر

**مثال 1:**
```
ADD_SCORE Ahmed 100
ADD_SCORE Sara 200
ADD_SCORE Ali 150
TOP 2
RANK Ahmed
ADD_SCORE Ahmed 150  (total = 250)
TOP 2
```
```
Output:
TOP 2 -> [Sara(200), Ali(150)]
RANK Ahmed -> 3
TOP 2 -> [Ahmed(250), Sara(200)]
```

**خطوات الحل (فكّر كده):**

1. **التخزين:** HashMap<String, Integer> للأسماء والنقاط (عشان التحديث يكون O(1)).
2. **أعلى k لاعبين:** ممكن PriorityQueue (Min Heap بحجم k). أو: آخد كل الـ entries وأعملها sort. لو الـ Leaderboard مش كبير، الـ sort هيبقى كويس.
3. **ترتيب لاعب:** عد كام لاعب نقاطه أعلى + 1. ممكن تستخدم TreeMap مع reversed order عشان تسهل العملية.
4. **لو عايز حل أحسن:** TreeMap<Integer, Set<String>> مقلوبة (أكبر نقاط الأول). بس خلي بالك لو اتنين بنفس النقاط.
5. **الدرس:** المشاكل الحقيقية بتحتاج Trade-offs — إيه العملية اللي محتاج تكون أسرع؟ على أساسها تختار الـ Data Structure.

---

## مسألة 39: تصميم نظام Autocomplete

**المسألة:**
صمم نظام Autocomplete. عندك ليستة من الكلمات مع عدد البحث عنها. لما المستخدم يكتب prefix، رجّعله أكتر 3 كلمات بتبدأ بالـ prefix ده وليها أعلى عدد بحث.

**المدخل:** `HashMap<String, Integer>` (كلمات وعدد البحث) و String prefix
**المخرج:** `List<String>` (أعلى 3)

**مثال 1:**
```
Words: {apple=100, app=200, application=50, banana=300, apply=80}
Prefix: "app"
Output: [app, apple, apply]
```

**مثال 2:**
```
Words: {car=50, card=30, care=40, cat=60}
Prefix: "car"
Output: [car, care, card]
```

**خطوات الحل (فكّر كده):**

1. **الخطوة الأولى:** فلتر الكلمات اللي بتبدأ بالـ prefix. ألف على الـ Map وأشيك `word.startsWith(prefix)`.
2. **الخطوة التانية:** من الكلمات المفلترة، لاقي أعلى 3. ممكن أستخدم PriorityQueue (Min Heap بحجم 3).
3. **أو:** أحط المفلتر في List، أعمل sort بالعدد تنازلياً، وآخد أول 3.
4. **لو عايز Optimization:** TreeMap بدل HashMap. الـ TreeMap بتقدر تعملها `subMap(prefix, prefix + Character.MAX_VALUE)` — هتديك بس الكلمات اللي بتبدأ بالـ prefix بدون ما تلف على كل الـ Map!
5. **لو عايز أسرع كمان:** Trie data structure — بس ده خارج نطاق Collections العادية.
6. **الدرس:** اختيار الـ Collection الصح (HashMap vs TreeMap) بيفرق جداً في الأداء.

---

## مسألة 40: تصميم Social Network Feed

**المسألة:**
صمم نظام Feed لشبكة اجتماعية. كل user ممكن يتابع users تانيين، وكل user بيعمل posts. المطلوب لما user يطلب الـ Feed بتاعه، يشوف آخر 10 posts من الناس اللي بيتابعهم مرتبة بالوقت (الأحدث الأول).

**المدخل:** أوامر FOLLOW, POST, GET_FEED
**المخرج:** نتائج الأوامر

**مثال 1:**
```
FOLLOW Ahmed Sara
FOLLOW Ahmed Ali
POST Sara "Hello World" t=1
POST Ali "Good Morning" t=2
POST Sara "Java is great" t=3
POST Omar "Nobody sees me" t=4
GET_FEED Ahmed
```
```
Output: ["Java is great"(t=3), "Good Morning"(t=2), "Hello World"(t=1)]
(Ahmed بيتابع Sara و Ali بس — مش Omar)
```

**خطوات الحل (فكّر كده):**

1. **التخزين:**
   - **Following:** `HashMap<String, HashSet<String>>` — كل user والناس اللي بيتابعهم (HashSet عشان البحث سريع والإضافة/الإزالة O(1)).
   - **Posts:** `HashMap<String, ArrayList<Post>>` — كل user والـ posts بتاعته. الـ Post فيه text و timestamp.
2. **FOLLOW:** `followMap.computeIfAbsent(user, k -> new HashSet<>()).add(target)`.
3. **POST:** أضيف Post جديد في ليستة الـ user.
4. **GET_FEED — الجزء الصعب:**
   - أجيب كل الناس اللي الـ user بيتابعهم من الـ followMap.
   - لكل واحد فيهم آخد الـ posts بتاعته.
   - **المشكلة:** إزاي أجيب آخر 10 من كل الليستات دي مرتبين؟
   - **الحل:** PriorityQueue (Max Heap على الـ timestamp). أحط كل الـ posts من كل الناس المتابعين في الـ Queue (أو أحط آخر post من كل واحد وأمشي واحدة واحدة — أذكى).
   - أسحب أول 10.
5. **الحل الأذكى (Merge k sorted lists):** لو posts كل user مرتبة بالوقت، أستخدم Min Heap بحجم = عدد المتابعين. أحط آخر post من كل user. أسحب الأحدث وأحط اللي قبله من نفس الـ user. وهكذا لحد ما أجيب 10.
6. **الـ Collections المستخدمة:** HashMap, HashSet, ArrayList, PriorityQueue — كلهم مع بعض!
7. **الدرس:** المشاكل الحقيقية بتحتاج تجمع أكتر من Collection وتختار الصح لكل جزء.

---

---

# 🎯 ملخص سريع — إمتى تستخدم إيه

| الموقف | الـ Collection المناسب |
|--------|----------------------|
| محتاج ترتيب ووصول بالـ index | **ArrayList** |
| محتاج إضافة/حذف كتير من الأول | **LinkedList** |
| محتاج عناصر فريدة (بدون تكرار) | **HashSet** |
| محتاج عناصر فريدة ومرتبة | **TreeSet** |
| محتاج عناصر فريدة بترتيب الإدخال | **LinkedHashSet** |
| محتاج key-value pairs | **HashMap** |
| محتاج key-value مرتبة بالـ key | **TreeMap** |
| محتاج key-value بترتيب الإدخال/الوصول | **LinkedHashMap** |
| محتاج FIFO (أول ما يدخل أول ما يخرج) | **Queue (LinkedList / ArrayDeque)** |
| محتاج LIFO (آخر ما يدخل أول ما يخرج) | **Stack (ArrayDeque)** |
| محتاج أعلى/أقل أولوية | **PriorityQueue** |
| محتاج إضافة/حذف من الطرفين | **Deque (ArrayDeque)** |

---

# 💡 نصايح عامة لحل المشاكل

1. **إقرأ المشكلة 3 مرات** قبل ما تبدأ تكتب كود.
2. **حدد الـ Input والـ Output** بالظبط — إيه اللي داخل وإيه اللي المفروض يطلع.
3. **فكّر في الـ Edge Cases:** ليستة فاضية؟ عنصر واحد؟ كل العناصر متساوية؟ أرقام سالبة؟
4. **اختار الـ Data Structure الصح:** بناءً على العمليات اللي محتاجها (بحث؟ ترتيب؟ تكرار؟).
5. **اكتب الخطوات بالعربي الأول** قبل ما تكتب كود.
6. **جرّب بالأمثلة يدوي** على ورقة قبل ما تكتب كود.
7. **ابدأ بأبسط حل** وبعدين حسّنه لو محتاج.
8. **الـ Patterns بتتكرر:** عد + إيجاد الأكبر، Two Pointers, Sliding Window, HashMap للبحث السريع، Stack للأقواس والـ undo... لما تحفظ الـ patterns هتحل أسرع.

---

> **🔥 تحدي ليك:** حل كل المسائل دي بالترتيب. لو حليت الـ 40 كلهم وفهمتهم كويس، أنت كده فاهم Java Collections أحسن من 90% من المبرمجين!

---

*تم إعداد هذا الملف ليكون مرجعك الشامل لحل مشاكل Java Collections*
