## 💡 متى تستخدم الـ `if-else if-else`؟

الـ `if` هي الأداة **الأكثر مرونة** والـ **Default** اللي بتستخدمها في الحالات دي:

- **مقارنة نطاقات (Ranges):** لو الشرط بتاعك بيقارن مدى من القيم، مش قيمة واحدة محددة
- **مقارنة أكتر من متغير:** لو الشرط بيعتمد على قيم مختلفة في نفس الوقت.
    - **مثال:** `if (userRole == 'ADMIN' && isActive) { ... }`
- **استخدام الـ Logical Operators:** لو بتحتاج الـ `&&` (AND) أو الـ `||` (OR) لربط الشروط.
- **أنواع غير مدعومة في `switch`:** لو الشرط بتاعك بيعتمد على قيمة **`long`** أو **`float`** أو **`double`** أو **`boolean`** (لأن الـ `switch` مابتدعمش الأنواع دي).

---

## 🚀 متى تستخدم الـ `switch`؟
الـ `switch` بيستخدم لما تكون عايز تختبر قيمة **متغير واحد بس** مقابل **قائمة محددة** من القيم (Discrete Values).
- **قائمة قيم محددة:** الأفضل استخدامها لما يكون عندك 4 أو 5 حالات مختلفة أو أكتر لنفس المتغير (زي الـ Status Codes أو الـ Command Types).
- **وضوح الكود:** بتخلي الكود بتاعك **أفضل قراءة** بكتير من سلسلة طويلة من الـ `if-else if`.
- **الأنواع المدعومة:** الـ `switch` بتدعم الـ Primitives (زي `byte`, `short`, `char`, `int`) وكمان الـ **`String`** والـ **`Enum`** (وهي دي اللي بتشوفها كتير في الـ ERP).
---

### 📝 الخلاصة

- **استخدم `if`:** للشروط المعقدة، أو مقارنات النطاقات، أو لما تحتاج أنواع بيانات مش مدعومة.
    
- **استخدم `switch`:** للتبديل السريع بين **قيم ثابتة ومحددة** لمتغير واحد.
  
  ---
- شكل الswitch 
  ```java
  int day = 3;

switch (day) {
    case 1:
        System.out.println("Sat");
        break;
    case 2:
        System.out.println("Sun");
        break;
    case 3:
        System.out.println("Mon");
        break;
    default:
        System.out.println("Unknown");
}
  ```
الشكل الجديد (Enhanced Switch) — من Java 14+
```java
switch (day) {
    case 1 -> System.out.println("Sat");
    case 2 -> System.out.println("Sun");
    case 3 -> System.out.println("Mon");
    default -> System.out.println("Unknown");
}
```
استخدام switch كـ expression:
```java
String type = switch (score) {
    case 90, 100 -> "Excellent";
    case 80, 89 -> "Very Good";
    case 70, 79 -> "Good";
    default -> "Below Average";
};
```
### ✔️ 1. الفرق بين if و switch؟ إمتى تستخدم كل واحدة؟

- if: للـ conditions اللي فيها عمليات (> < != && …)
    
- switch: لما تكون القيم ثابتة ومعروفة (ENUM / int / String)
- ### ✔️ 3. ينفع أستخدم switch مع String؟

أيوه… من Java 7.

### ✔️ 4. ينفع أستخدم switch مع boolean؟

لأ… boolean مش مدعوم إلا لو في شكل enum .

### ✔️ 5. ينفع أستخدم switch مع objects؟

لأ… لازم القيم تكون primitives أو String أو enum.

### ✔️ 6. الفرق بين fall-through و break؟

- fall-through: تنفيذ case بعد case لو مفيش break
    
- break: بيمنع الانتقال للـ case اللي بعده


7 
what the output of 
```java
int x = 2;

switch(x) {
    case 1:
        System.out.println("A");
    case 2:
        System.out.println("B");
    case 3:
        System.out.println("C");
}
```
b c 
التركة: **مفيش break → fall-through**

8
```java
int x = 3;

switch(x) {
    default:
        System.out.println("D");
    case 1:
        System.out.println("A");
    case 2:
        System.out.println("B");
}
```
DAB