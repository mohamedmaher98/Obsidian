# 🧱 SQL Server Architecture Overview

SQL Server مكون من 3 أجزاء رئيسية، كل جزء له دور مهم في معالجة البيانات وتشغيل الأوامر.

---

## 1️⃣ Relational Engine (Query Processor)

- 🧠 "العقل المفكر"
- مسؤول عن:
  - تحليل أوامر SQL وفهمها
  - إنشاء خطة تنفيذ (Query Plan)
  - تنفيذ الأوامر
- لا يتعامل مع الملفات مباشرة، بل يطلب البيانات من الـ Storage Engine.

---

## 2️⃣ Storage Engine

- 💾 مسؤول عن:
  - تخزين البيانات فعليًا على الـ Disk
  - تنفيذ أوامر القراءة والكتابة
  - التعامل مع ملفات:
    - `.mdf` (ملف البيانات الرئيسي)
    - `.ldf` (ملف العمليات أو اللوج)
- هو اللي بيوصل للبيانات فعليًا ويخزنها أو يجيبها.

---

## 3️⃣ SQL OS (SQL Server Operating System)

- ⚙️ نظام تشغيل داخلي داخل SQL Server
- مسؤول عن:
  - إدارة الذاكرة (Memory)
  - تنظيم المعالجة والمهام (Scheduling)
  - إدارة الـ I/O (الدخول/الخروج من الملفات)
- يشتغل فوق Windows OS لكن بيوفر طبقة تحكم أدق.

---

## 🔁 دورة العمل:

1. المستخدم يكتب كود SQL (مثل: `SELECT * FROM Employees`)
2. الكود يروح لـ **Relational Engine**
3. الـ Engine يحلل ويقرر أفضل طريقة لتنفيذه
4. يطلب البيانات من **Storage Engine**
5. البيانات تُرجع للمستخدم

---

## ✅ ملخص بصري:

```text
[User Query]
      ↓
[Relational Engine]
      ↓
[Storage Engine] ←→ [Database Files]
      ↓
   [Result to User]
