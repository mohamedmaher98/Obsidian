لـ `INNER JOIN` في SQL بيستخدم عشان نجيب بيانات من جدولين (أو أكتر) بشرط إن يكون في علاقة (مشتركة) ما بينهم.

بمعنى تاني:  
بنربط جدولين على عمود مشترك، وبنرجع **بس** الصفوف اللي فيها تطابق في العمود ده.

### 🧰 استخداماته:

- لما تحب تربط بيانات من أكتر من جدول عندهم علاقة ببعض (زي الموظف والقسم، الطالب والكورس، الفاتورة والعملاء).
    
- استخراج تقارير مركبة فيها بيانات من أكتر من مصدر.
    
- بناء views مركبة في قواعد البيانات.
  
  **`INNER JOIN`**
- بيرجع **فقط** الصفوف اللي ليها تطابق في الجدولين.  
لو عايز ترجع الكل، حتى اللي مالهمش تطابق، وقتها تستخدم `LEFT JOIN` أو `RIGHT JOIN`.