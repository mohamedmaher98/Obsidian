# 🛠️ SQL Server Services Overview

---

## ✅ ما المقصود بـ Services في SQL Server؟

- **Services = خدمات خلفية** بتشتغل مع تشغيل SQL Server.
- كل خدمة مسؤولة عن وظيفة معينة داخل النظام.
- بتتحكم فيها من خلال "SQL Server Configuration Manager" أو "Windows Services".

---

## 🔧 أهم خدمات SQL Server:

### 1️⃣ SQL Server (MSSQLSERVER / Instance Name)

- 🧠 الخدمة الأساسية.
- مسؤولة عن تشغيل **Database Engine**.
- بتنفذ أوامر SQL وتدير قواعد البيانات.
- بدونها مش هيشتغل SQL Server أساسًا.

---

### 2️⃣ SQL Server Agent

- ⏰ خاص بـ المهام المجدولة (Jobs).
- يشغل Scripts أو عمليات Backup تلقائيًا.
- لازم يكون مفعّل لو هتستخدم Automation داخل السيرفر.

---

### 3️⃣ SQL Server Browser

- 🔍 بيخلي المستخدمين يكتشفوا الـ Named Instances بسهولة.
- بيوضح أي Instance شغالة على أي Port.
- مفيد لما يكون فيه أكتر من Instance.

---

### 4️⃣ SQL Server Full-Text Search

- 🧾 خدمة بتسمح بعمليات بحث متقدم داخل النصوص.
- مفيدة في أنظمة CMS أو البحث في وصف المنتجات.

---

### 5️⃣ SQL Server Reporting Services (SSRS)

- 📊 بتستخدم لإنشاء ونشر تقارير من البيانات.
- بتشتغل بشكل منفصل لكن تتكامل مع قواعد البيانات.

---

## 📋 مقارنة سريعة:

| الخدمة                        | الوظيفة                                       | أساسية؟ |
|------------------------------|-----------------------------------------------|---------|
| **SQL Server Engine**        | تشغيل القواعد وتنفيذ الأوامر                  | ✅       |
| **SQL Server Agent**         | جدولة المهام وتشغيلها تلقائيًا               | ❌       |
| **SQL Server Browser**       | تسهيل الاتصال بالـ Instances المختلفة        | ❌       |
| **Full-Text Search**         | بحث متقدم داخل محتوى النصوص                 | ❌       |
| **Reporting Services (SSRS)**| إنشاء وعرض تقارير                            | ❌       |

---

## 🎯 أهمية معرفة الخدمات:

- لو السيرفر مش شغال → تأكد إن **Database Engine** مفعّل.
- لو الـ Job مش شغالة → فعّل **SQL Server Agent**.
- لو عندك أكثر من Instance → تأكد إن **Browser** شغال.

