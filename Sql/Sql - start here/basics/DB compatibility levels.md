# 🔁 Compatibility Levels in SQL Server – شرح بلدي

---

## ✅ يعني إيه Compatibility Level؟

- هو "درجة التوافق" اللي قاعدة البيانات شغالة بيها.
- بيخلّي القاعدة **تشتغل كأنها على إصدار SQL أقدم**، حتى لو السيرفر نفسه أحدث.

> 📌 زي ما تكون مركّب Windows 11، لكن بتخلي برنامج يشتغل كأنه على Windows 7.

---

## 🎯 ليه بنحتاج الـ Compatibility Level؟

- لو عندك قاعدة بيانات قديمة وعايز تشغلها على سيرفر جديد.
- بدل ما تكسر الدنيا، SQL Server يسمح للقاعدة تشتغل بـ "طريقتها القديمة".
- ده يحافظ على تشغيل الكود القديم من غير تعديل.

---

## 🔢 أمثلة على مستويات التوافق:

| Compatibility Level | كأنها شغالة على إصدار...     |
|---------------------|-------------------------------|
| 160                 | SQL Server 2022               |
| 150                 | SQL Server 2019               |
| 140                 | SQL Server 2017               |
| 130                 | SQL Server 2016               |
| 120                 | SQL Server 2014               |

---

## 🔍 أعرف المستوى إزاي؟

```sql
SELECT name, compatibility_level
FROM sys.databases;
```

## ⚙️ أغيّر المستوى إزاي؟

`ALTER DATABASE MyDB SET COMPATIBILITY_LEVEL = 160;`

✏️ غير `MyDB` لاسم قاعدتك  
✏️ واختار المستوى المناسب (150, 140, 160...)

---

## 💡 شرح بلدي سريع:

> السيرفر = عقل حديث  
> القاعدة = ممكن تكون بتفكر بطريقة قديمة  
> الكومباتيبيليتي بيساعدهم يفهموا بعض

---

## ⚠️ خليك عارف:

- بعض الأوامر الجديدة مش هتشتغل إلا لو المستوى عالي كفاية.
    
    - مثال: `STRING_AGG()` يشتغل من مستوى 140 فقط.
        
- لما تطمّن إن كل الاستعلامات بتشتغل، ارفع الـ level عشان تستفيد بالمميزات الجديدة.
    

---

## 🔐 ملاحظات سريعة:

- لما تنقل قاعدة من سيرفر قديم → خليك على نفس الـ level مؤقتًا.
    
- لما تبدأ مشروع جديد على SQL حديث → استخدم أعلى مستوى متاح.