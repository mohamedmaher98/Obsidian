# 🔢 SQL Server Numeric Data Types – شرح بلدي

---

## ✅ يعني إيه Numeric Data Type؟

- نوع من أنواع الأعمدة (Columns) في الجداول.
- بيحدد إنت ناوي تخزن **نوع إيه من الأرقام**؟
  - أرقام صحيحة؟
  - أرقام عشرية؟
  - أرقام صغيرة ولا كبيرة؟

---

## 📦 أولًا: الأرقام الصحيحة (Integer Types)

| النوع      | بيخزن من كام لـ كام؟                   | الحجم في الذاكرة | تستخدمه إمتى؟                             |
|------------|------------------------------------------|------------------|--------------------------------------------|
| `tinyint`  | من 0 لـ 255                              | 1 بايت           | لو عندك رقم صغير جدًا (زي أيام الأسبوع)    |
| `smallint` | من -32,768 لـ 32,767                     | 2 بايت           | لو رقم صغير وممكن يبقى سالب                |
| `int`      | من -2,147,483,648 لـ 2,147,483,647       | 4 بايت           | النوع الافتراضي لمعظم الأرقام              |
| `bigint`   | من -9 كوينتليون لـ +9 كوينتليون تقريبًا | 8 بايت           | لو عندك أرقام ضخمة جدًا (زي عدد مشاهدات)  |

> 💡 كل ما الرقم أكبر → كل ما حجم الذاكرة يزيد

---

## 📊 ثانيًا: الأرقام العشرية (Decimal Types)

| النوع        | الوصف                                       | تستخدمه إمتى؟                                  |
|-------------|----------------------------------------------|--------------------------------------------------|
| `decimal(p,s)` | p = عدد الخانات، s = عدد الخانات بعد العلامة | لو عايز تتحكم في الدقة (زي أسعار، حسابات فلوس) |
| `numeric(p,s)` | نفس فكرة decimal تمامًا                     | الاتنين interchangeable                        |

> 💡 مثال:

الdecimal(5,2) → يخزن رقم زي 123.45 (5 خانات، 2 بعد العلامة)
## 🌊 ثالثًا: الأرقام التقريبية (Approximate Types)

|النوع|الوصف|تستخدمه إمتى؟|
|---|---|---|
|`float`|بيخزن أرقام عشرية بدقة تقريبية (مش ثابتة)|لو مش محتاج دقة 100% (زي درجات حرارة)|
|`real`|نسخة أخف من float (حجم أقل)|لو المساحة تهمك ومش فارقة الدقة العالية|

> 💡 float و real أسرع، لكن مش دايمًا يحفظ الرقم بالضبط  
> يعني 1.000000001 ممكن يتسجل 1.0000000 بس

---

## 🎯 تستخدم أنهي نوع إمتى؟

|الحالة|النوع المناسب|
|---|---|
|عداد بسيط / رقم ID صغير|`int` أو `smallint`|
|مبلغ مالي بدقة معينة|`decimal` أو `numeric`|
|رقم ضخم جدًا (زي عدد زيارات)|`bigint`|
|حاجة تقريبية زي السرعة|`float` أو `real`|
|عدد محدود من الخيارات (0–255)|`tinyint`|

---

## 🧪 مثال عملي:

`CREATE TABLE Products (     ProductID int,     Price decimal(10,2),     StockQuantity smallint,     Rating float );`

---

## ⚠️ ملاحظات بلدي:

- لو بتتعامل مع **فلوس أو حسابات دقيقة** → استخدم `decimal` أو `numeric`
    
- لو بتخزن **قيم تقريبية** مش هتأثر على النتيجة → استخدم `float` أو `real`
    
- حاول تختار أصغر نوع يكفي البيانات عشان الأداء والمساحة