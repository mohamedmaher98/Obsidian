

##  Implicit Casting (التحويل الضمني)
- بيحصل تلقائي من **نوع أصغر لنوع أكبر**.
- آمن ومفيهوش فقد بيانات.
- ✅ Java بتعمله من غير تدخل منك.

### 🧪 مثال:
```java
int a = 10;
double b = a; // 10 → 10.0
System.out.println(b); // 10.0
```

## ✅ Explicit Casting (التحويل الصريح)

- لازم تكتبه بإيدك.
    
- بيحصل لما تحوّل من **نوع أكبر لنوع أصغر**.
    
- ممكن يسبب **فقد بيانات** أو **نتايج غير متوقعة**.
    
- ⛔ Java مش هتعمله لوحدها.
    

### 🧪 مثال:

`double x = 9.7; int y = (int) x; // y = 9 System.out.println(y); // 9`

## ⚠️ ملاحظات مهمة:

- التحويل من double → int أو long → short لازم يتم يدويًا باستخدام (type).
    
- التحويل الضمني بيشتغل من:
    
    - byte → short → int → long → float → double
      
      public class TypeCastingExample {
    public static void main(String[] args) {
        int a = 10;
        double b = a; // Implicit

        double x = 9.8;
        int y = (int) x; // Explicit

        System.out.println("b = " + b); // 10.0
        System.out.println("y = " + y); // 9
    }
}
``## 🧠 قاعدة تفكرك:

> من الصغير للكبير → Java بتحوّل لوحدها  
> من الكبير للصغير → لازم تقولها بنفسك

