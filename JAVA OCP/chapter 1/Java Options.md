# 🧰 Java Options — ملخص منظم

## 🎯 يعني إيه Java Options؟

هي **Parameters** بنمرّرها للـ JVM وقت تشغيل البرنامج (من Command Line أو إعدادات السيرفر).  
دورها إنها تتحكم في **سلوك JVM** من ناحية:

- إدارة الذاكرة Memory Management
    
- تحسين الأداء Performance
    
- ضبط أنواع الـ Garbage Collector
    
- تحديد أماكن الملفات والمسارات Classpaths
    

---

# 🧠 أهم الخيارات الخاصة بالذاكرة (Heap)

## 🔹 `-Xmx`

**Maximum Heap Size**  
أقصى حجم للـ Heap مسموح للـ JVM يستخدمه.  
البرنامج **ميفضّلش** يتخطاه.

## 🔹 `-Xms`

**Initial Heap Size**  
الحجم الأولي للـ Heap عند بدء تشغيل التطبيق.

## 👍 ليه نخلي `-Xms` = `-Xmx` ؟

لأن لو الاتنين مش قد بعض:

- الـ JVM هتفضل **تكبّر الـ Heap تدريجي** من Xms لحد Xmx
    
- عملية التكبير بتستهلك CPU
    
- وبتسبب **Pauses** عند الـ Garbage Collection
    
- فالنتيجة: **Performance أقل**
    

أما لو الاتنين قد بعض → مفيش تمدد → مفيش Pauses زيادة → أداء ثابت.

---

# 🧹 GC Pauses — ليه بتحصل؟

الـ JVM لما تنظّف الذاكرة، ساعات بتحتاج توقف البرنامج لحظات:  
ده اسمه **Stop-The-World Pause**.

السبب: الـ GC محتاج يرتّب ويفرّغ أماكن في الذاكرة.

---

# 🗂️ تقسيمة الـ Heap: Young vs Old

الـ JVM بتقسّم الـ Heap لـ 2 Generations:

---

## 👶 1. Young Generation

مكان تكوّن **كل Objects الجديدة**.

خصائصها:

- الـ JVM متوقع 90% من الـ Objects دي **بتموت بسرعة**.
    
- بيحصل لها تنظيف سريع ومتكرر: **Minor GC**
    
- الهدف: تقليل الـ Pauses قدر الإمكان.
    

---

## 👴 2. Old Generation

لما الـ Objects تعيش فترة طويلة، تتنقل هنا.

خصائصها:

- تنظيفها اسمه **Major GC** أو **Full GC**
    
- أقل تكرارًا لكنه أبطأ
    
- وهو السبب الأكبر في الـ Pauses الطويلة
    

---

# 📈 إمتى الـ Object يترقّى من Young لـ Old؟

ده بيحصل عن طريق آلية اسمها:

## 🔢 Tenuring Threshold (عتبة الترقية)

آلية العمل:

1. **كل Object بيتخلق** في Young ومعاه **عداد صغير**.
    
2. كل مرة ينجو من Minor GC → العداد **يزيد 1**.
    
3. لما العداد يوصل للـ Threshold → يتم **Promotion** للـ Old Gen.
    

⚠️ الـ JVM **متعرفش** إن ده Cache أو Session  
هو بيقرر الترقية حسب _عُمر الـ Object_.

---

# 📌 خلاصة سريعة

- استخدم `-Xms` = `-Xmx` لضمان أداء ثابت.
    
- Young Gen = سريعة ومناسبة للـ Objects اللي بتموت بسرعة.
    
- Old Gen = بطيئة وتنظيفها بيعمل Pauses أطول.
    
- Promotion بيحصل لما الـ Object يعيش عدد مرات كفاية بعد الـ GC.

---
# ملخص أنواع الـ Garbage Collectors الرئيسية

الفرق الأساسي بين أنواع الـ GC هو **التوازن بين:**

- **Throughput** → سرعة تنفيذ البرنامج بالكامل
    
- **Latency** → مدة التوقف اللحظي (GC Pause)
    

---

## 🟦 1) Parallel GC

**Option:**

`-XX:+UseParallelGC`

**الهدف الأساسي:**  
✨ أعلى Throughput ممكن

**المميزات:**

- 🟢 سريع جداً في جمع الذاكرة
    
- 🟢 بيستغل الـ CPU بأقصى شكل
    

**العيوب:**

- 🔴 بيسبب **توقفات طويلة** (Long GC Pauses)
    

---

## 🟧 2) G1 GC (الافتراضي حالياً)

**Option:**

`-XX:+UseG1GC`

**الهدف الأساسي:**  
⚖️ توازن ممتاز بين السرعة والـ Latency

**المميزات:**

- 🟡 تقسيم الذاكرة لـ **Regions** → يخلي التنظيف أذكى
    
- 🟢 ممتاز للـ **Enterprise Applications**
    
- 🟢 مناسب للـ Heaps الكبيرة
    

**العيوب:**

- 🔶 مش Ultra-Low Latency زي ZGC/Shenandoah
    
- 🔶 بيحتاج إعدادات جيدة للاستفادة القصوى منه
    

---

## 🟩 3) ZGC / Shenandoah

**Options:**

`-XX:+UseZGC`

أو

`-XX:+UseShenandoahGC`

**الهدف الأساسي:**  
⚡ Ultra-Low Latency (وقفات أقل من **10ms**)

**المميزات:**

- 🟢 بيشتغل Concurrent مع الكود
    
- 🟢 GC Pauses قليلة جداً
    

**العيوب:**

- 🔴 استهلاك CPU أعلى من G1
    
- 🔴 مش محتاجه إلا لو بتشتغل على أنظمة Ultra-Low Latency


# حاجات لازم تعرفها دلوقتي (ضرورية)

دي أساسية لأي Backend Java Developer:

### 🔹 إدارة الذاكرة:

- `-Xms`
    
- `-Xmx`
    
- `-Xss`
    
- فكرة الـ Heap والـ Stack
    

### 🔹 أنواع GC الأساسية:

- Parallel GC
    
- G1 GC
    
- والأسامي الكبيرة زي ZGC/Shenandoah تعرفهم “بالاسم بس”
    

### 🔹 Debug Options:

- `-Xlog:gc`
    
- `-XX:+HeapDumpOnOutOfMemoryError`
    

### ليه؟

عشان لو السيرفر وقع أو البرنامج بيهنج — تبقى فاهم تعمل إيه.
