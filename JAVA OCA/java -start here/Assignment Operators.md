

## 📝 تعريف بسيط

الـ **Assignment Operators** في Java هي الطرق اللي بتستخدمها عشان "تدي" قيمة لمتغير.

أبسط مثال هو = اللي بنستخدمه كل يوم:

```java
int x = 5;
```

ده معناه إنك بتقول: "يا x خزنلي جواك الرقم 5".

---

## 📌 أنواع Assignment Operators

فيه مجموعة Operators ممكن تريحك وتختصر الكود بدل ما تكتب نفس المتغير مرتين.

### ✅ =`

أبسطهم، بيخزن قيمة في متغير:

```java
int a = 10;
```

### ✅ `+=`

بيزود قيمة على اللي موجود بالفعل:

```java
int a = 5;
a += 3;  // نفس كأنك كتبت a = a + 3;
System.out.println(a); // 8
```

### ✅ `-=`

بيطرح من القيمة الموجودة:

```java
int a = 10;
a -= 4;  // نفس كأنك كتبت a = a - 4;
System.out.println(a); // 6
```

### ✅ `*=`

بيضرب القيمة:

```java
int a = 2;
a *= 5;  // نفس كأنك كتبت a = a * 5;
System.out.println(a); // 10
```

### ✅ `/=`

بيقسم القيمة:

```java
int a = 20;
a /= 4;  // نفس كأنك كتبت a = a / 4;
System.out.println(a); // 5
```

### ✅ `%=`

بيجيب الباقي (modulus):

```java
int a = 10;
a %= 3;  // نفس كأنك كتبت a = a % 3;
System.out.println(a); // 1
```

---

## ⚠️ ملاحظات

- النوع ده من الـ operators بيساعد في تقليل الكود وتبسيطه.
    
- لازم يكون المتغير على **الشمال** من الـ operator.
    
- اشتغل كويس مع الأرقام (int, double...) بس مش بينفع تستخدمهم مع كل أنواع البيانات.
    

---

## 🔗 نوتات مرتبطة

- [[Arithmetic Operators]]
    
- [[Data Types]]
    
- [[Variable Declaration and Initialization]]