# 📦 Creating Databases in SQL Server

---

## ✅ ما المقصود بإنشاء قاعدة بيانات؟

- إعداد مساحة داخل SQL Server لتخزين البيانات.
- تتخزن فعليًا في ملفات:
  - `.mdf` → ملف البيانات
  - `.ldf` → ملف اللوج

---

## 🟩 الطريقة 1: باستخدام T-SQL

```sql
CREATE DATABASE MyFirstDB;
```

- اسم القاعدة يجب أن يكون فريدًا داخل الـ Instance.
    
- يُفضل استخدام أسماء واضحة ومعبرة.
    
- لو الاسم يحتوي على مسافات، يجب وضعه بين `[ ]`: