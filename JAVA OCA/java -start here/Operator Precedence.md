# Operator Precedence

## 🎯 يعني إيه "أولوية العمليات"؟

في Java، لما تكتب تعبير فيه أكتر من operator، المترجم (compiler) بيحتاج يعرف يشتغل بأي operator الأول.

**Operator Precedence** معناها ترتيب أولوية تنفيذ العمليات.

يعني مثلاً:

```
int result = 3 + 4 * 2;
```

الضرب (`*`) ليه أولوية أعلى من الجمع (`+`)، فالمترجم هيحسب 4 * 2 الأول، بعدين يجمع عليه 3 → النتيجة = 11.

---

## 📋 ترتيب الأولويات (من الأعلى للأقل)

| الأولوية | الأمثلة                            | النوع                |            |            |
| -------- | ---------------------------------- | -------------------- | ---------- | ---------- |
| 1        | `()`                               | الأقواس - تنفذ أولًا |            |            |
| 2        | `++`, `--`, `+`, `-`, `!`, `~`     | Unary Operators      |            |            |
| 3        | `*`, `/`, `%`                      | الضرب والقسمة        |            |            |
| 4        | `+`, `-`                           | الجمع والطرح         |            |            |
| 5        | `<<`, `>>`, `>>>`                  | Bitwise Shift        |            |            |
| 6        | `<`, `>`, `<=`, `>=`, `instanceof` | المقارنات            |            |            |
| 7        | = =, !=                            | المقارنة بين القيم   |            |            |
| 8        | `&`                                | Bitwise AND          |            |            |
| 9        | `^`                                | Bitwise XOR          |            |            |
| 10       | `                                  | `                    | Bitwise OR |            |
| 11       | `&&`                               | Logical AND          |            |            |
| 12       | `                                  |                      | `          | Logical OR |
| 13       | `? :`                              | Ternary Operator     |            |            |
| 14       | `=`, `+=`, `-=`, `*=`, `/=`, `%=`  | Assignment           |            |            |

---

## ⚠️ ملاحظات

- الأقواس `()` بتقدر تتحكم بالأولوية زي ما تحب.
    
    ```
    int x = (3 + 4) * 2; // النتيجة = 14
    ```
    
- لو في اتنين من نفس الأولوية، Java تمشي من الشمال لليمين (ما عدا في بعض الحالات زي =).
    

---

## 🧠 نصيحة

حتى لو عارف ترتيب الأولويات، استخدام الأقواس دايمًا بيخلي الكود أوضح وأسهل في القراءة والصيانة.

---