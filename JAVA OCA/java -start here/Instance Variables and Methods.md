

## 📝 تعريف بسيط
في Java، الـ **instance variables** و **instance methods** هما الحاجات اللي بتخص كل كائن (object) بيتعمل من الكلاس.

يعني كل نسخة من الكلاس بيكون ليها **نسختها الخاصة** من المتغيرات والدوال دي.

---

## 📦 Instance Variables (المتغيرات الخاصة بالكائن)
هي المتغيرات اللي بتتعرّف جوه الكلاس **برا أي method**، وكل كائن بيكون ليه نسخة منها.

### 📌 مثال:
```java
public class Person {
    String name;  // ده instance variable
    int age;      // ده كمان

    void sayHello() {
        System.out.println("Hi, my name is " + name);
    }
}
```

### ✅ استخدام:
```java
Person p1 = new Person();
p1.name = "Ali";
p1.age = 25;

Person p2 = new Person();
p2.name = "Sara";
p2.age = 30;
```

كل كائن عنده بياناته الخاصة.

---

## ⚙️ Instance Methods (الوظايف الخاصة بالكائن)
هي الدوال اللي بتشتغل على بيانات الكائن نفسه، وبتقدر توصل للـ instance variables مباشرة.

### 📌 مثال:
```java
public class Person {
    String name;

    void sayHello() {
        System.out.println("Hello, my name is " + name);
    }
}
```

```java
Person p = new Person();
p.name = "Ziad";
p.sayHello(); // Hello, my name is Ziad
```

---

## 🧠 ملاحظات مهمة
- لو عندك 10 كائنات من نفس الكلاس، كل واحد ليه نسخته الخاصة من الـ instance variables.
- تقدر تستخدم `this` لو محتاج تفرق بين المتغير والكلمة اللي جايالك كـ باراميتر:
  ```java
  public void setName(String name) {
      this.name = name;
  }
  ```

---

## ❓ الفرق بين Instance و Static:
| المقارنة        | Instance              | Static                     |
|------------------|------------------------|------------------------------|
| تنتمي لـ         | الكائن                | الكلاس نفسه                 |
| الوصول ليها     | لازم object            | ممكن من غير object         |
| الذاكرة          | كل object ليه نسخة    | نسخة واحدة بس بتتشير       |

---

## 🔗 نوتات مرتبطة
- [[Class Definition]]
- [[Static Variables and Methods]]
- [[Object Creation and Memory Allocation]]
- [[this Keyword]]
