
# 🏷️ Labeled Break and Continue

## 📝 تعريف بسيط
في Java، ممكن تستخدم `break` و `continue` علشان تتحكم في تدفق اللوب.  
بس ساعات بتكون جوا لوب متعشّش (nested loops)، وعايز تكسر أو تكمل لوب خارجي مش الحالي،  
وساعتها Java بتديك ميزة إنك تحط **label** على اللوب وتستخدمه مع `break` أو `continue`.

وده بنسميه: **Labeled Break** أو **Labeled Continue**.

---

## 🏷️ إزاي بنكتب label؟
بتحط label قبل اللوب، وبتسميه بأي اسم (زي `outer:`)، وبعدين تستخدمه مع `break` أو `continue`.

---

## 📌 مثال: Labeled `break`
```java
outer:
for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= 3; j++) {
        if (i == 2 && j == 2) {
            break outer; // هيكسر اللوب الكبير كله
        }
        System.out.println("i = " + i + ", j = " + j);
    }
}
```
📤 الناتج:
```
i = 1, j = 1
i = 1, j = 2
i = 1, j = 3
i = 2, j = 1
```

ليه وقف؟ لأن لما `i = 2` و `j = 2` اتحققت، نفذ `break outer` وكسر اللوب كله.

---

## 📌 مثال: Labeled `continue`
```java
outer:
for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= 3; j++) {
        if (i == 2 && j == 2) {
            continue outer; // هيكمل اللوب الخارجي ويعدي الداخلي
        }
        System.out.println("i = " + i + ", j = " + j);
    }
}
```
📤 الناتج:
```
i = 1, j = 1
i = 1, j = 2
i = 1, j = 3
i = 2, j = 1
i = 3, j = 1
i = 3, j = 2
i = 3, j = 3
```

لما `i = 2` و `j = 2` اتحققت، نفذ `continue outer` وراح يكمل من أول اللوب الخارجي.

---

## ⚠️ ملاحظات مهمة
- ميزة قوية لكن مش منتشرة أوي، وتُستخدم لما يكون عندك **لوبات متعششة** ومحتاج تخرج أو تكمل لوب معين.
- خلي بالك: الكود هيكون أوضح وأسهل لما تسمي الـ label اسم معبر زي `outerLoop:`.

---

## 🧠 نصيحة بلدي
لو عندك لوب جوا لوب، وبتحاول تخرج من التاني مش الحالي، استخدم `break + label`  
ولو عايز تعيد اللوب الخارجي وتتخطى الحالي، استخدم `continue + label`.

---

## 🔗 نوتات مرتبطة
- [[Break and Continue]]
- [[Nested Loops]]
- [[Control Flow]]
