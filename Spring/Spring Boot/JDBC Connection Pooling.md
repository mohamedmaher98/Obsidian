

### ✅ يعني إيه Connection Pooling؟

بدل ما تفتح اتصال بقاعدة البيانات (Database) كل مرة من أول وجديد، بنعمل **تجميعة من الاتصالات الجاهزة** (Pool).

وهو الي بيتحكم في الاتصال والاغالاق للاتصاللات بالداتا بيز

لو مش ضايف انت الاعدادت بتاعت الكونكشنبول هو بيتسخدم الموجود فال tomcat
- الكود ياخد اتصال من الـ Pool
    
- يستخدمه
    
- يرجعه تاني للـ Pool
    

وده بيوفر وقت وموارد بشكل كبير.

---

### 🔧 إزاي بيشتغل؟

1. أول ما التطبيق يشتغل، بيتعمل شوية Connections.
    
2. أي كود محتاج DB بياخد Connection جاهز.
    
3. بعد ما يخلص، يرجعه تاني.
    
4. الاتصالات دي مش بتتقفل، بتتدوَّر.
    

---

### ✅ المميزات:

|الفايدة|الشرح|
|---|---|
|أداء أسرع|مش بتفتح Connection كل مرة|
|موارد أقل|عدد محدود من الاتصالات المفتوحة|
|تحكم سهل|بتقدر تحدد العدد الأقصى والأدنى|

---

### ⚙️ أشهر مكتبات Pooling:

|المكتبة|مدعومة في Spring Boot؟|مميزاتها|
|---|---|---|
|HikariCP|✅ (الافتراضي في Spring)|سريع وخفيف|
|Apache DBCP2|✅|مستقر|
|Tomcat JDBC Pool|✅|سهل مع Tomcat|

---

### ⚙️ إعداد HikariCP (في `application.properties`):

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/mydb
spring.datasource.username=root
spring.datasource.password=yourpass

#Hikari connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
```

---

### 🧪 مثال عملي:

```java
@Autowired
private DataSource dataSource;

public void testConnection() throws SQLException {
    Connection conn = dataSource.getConnection();
    // استخدم الاتصال هنا
    conn.close(); // بيرجع تلقائي للـ Pool
}
```

---

### 📌 ملخص:

- Connection Pooling = استخدام اتصالات DB جاهزة
    
- أسرع وأكفأ من فتح Connection كل مرة
    
- Spring Boot بيدعم HikariCP تلقائيًا


![[Pasted image 20250510211217.png]]