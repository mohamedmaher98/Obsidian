

## 📝 تعريف بسيط

الـ **Logical Operators** في Java بتستخدمهم علشان تجمع أو تربط بين أكتر من شرط. النتيجة دايمًا بتكون **true** أو **false**.

تخيلهم زي أدوات بتخليك تقول: "لو الشرط الأول صح وكمان التاني، أو لو واحد منهم على الأقل... الخ"

---

## 📌 أنواع Logical Operators

### ✅ `&&` (AND) - Logical AND

- بيرجع `true` **لو كل الشروط** اللي على الجانبين صح.
    
- أول ما يلاقي شرط كاذب على طول بيرجع `false` (اسمه short-circuit).
    

```java
int age = 25;
int salary = 6000;
System.out.println(age > 18 && salary > 5000); // true
```

### ✅ `&` (AND) - Bitwise AND مع boolean

- شبيه بـ `&&` بس **ما بيوقفش لو لقى شرط غلط**، بيكمل ينفذ كل الشروط.
    
- غالبًا مش هتستخدمه مع boolean إلا لو عايز تنفذ الشرطين دايمًا حتى لو الأول غلط.
    

```java
int a = 5, b = 10;
System.out.println(a > 1 & b++ > 5); // true والب بيرتفع
System.out.println(b); // 11
```

### ✅ `||` (OR) - Logical OR

- بيرجع `true` لو **واحد على الأقل** من الشروط صح.
    
- أول ما يلاقي شرط صح، بيوقف الباقي.
    

```java
int marks = 40;
System.out.println(marks >= 50 || marks == 40); // true
```

### ✅ `|` (OR) - Bitwise OR مع boolean

- شبيه بـ `||` لكن **بيكمل ينفذ الشروط كلها**.
    

```java
int x = 2, y = 3;
System.out.println(x > 1 | y++ > 1); // true
System.out.println(y); // 4
```

### ✅ `!` (NOT)

- بيقلب النتيجة، يعني لو الشرط `true` يرجعه `false` والعكس.
    

```java
boolean isLoggedIn = false;
System.out.println(!isLoggedIn); // true
```

---

## 🧪 مثال واقعي

```java
int age = 20;
boolean hasLicense = true;

if (age >= 18 && hasLicense) {
    System.out.println("يسمح له بالقيادة");
} else {
    System.out.println("غير مسموح");
}
```

---

## ⚠️ ملاحظات

- `&&` أسرع من `&` لأنه بيوقف أول ما يلاقي شرط غلط.
    
- `||` أسرع من `|` لأنه بيوقف أول ما يلاقي شرط صح.
    
- استخدم الأقواس `()` لما يكون في أكتر من شرط علشان تتحكم في الأولوية.
    
- لو مش محتاج تنفذ الشرطين مع بعض فعليًا، استخدم `&&` و `||` بدل `&` و `|`.
    

---

## 🔗 نوتات مرتبطة

- [[Comparison Operators]]
    
- [[Control Flow (if, else, switch)]]
    
- [[Boolean Data Type]]
    
- [[Operators in Java]]