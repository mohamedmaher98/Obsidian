### 🎯 تعريف الScope

Scope يعني "المدى" أو "نطاق الوصول" للمتغير داخل البرنامج: فين تقدر تستخدمه وتشوفه.

---

## 🔸 1. Local Variable (المتغير المحلي)

- بيتعرف **جوه method أو block**.
    
- **محدود** بالمكان اللي اتعرف فيه.
    
- لازم تهيّئه قبل الاستخدام.
    

```java
public void sayHello() {
    String message = "Hello";
    System.out.println(message);
}
```

⛔ مش هينفع تستخدم `message` برا الميثود.

---

## 🔸 2. Instance Variable (متغير الكائن)

- بيتعرف **جوه الكلاس لكن برا أي method**.
    
- بيخص **كل كائن (object)** من الكلاس.
    
- مش لازم تهيئه، بياخد قيمة افتراضية.
    

```java
public class Person {
    String name; // Instance variable

    public void printName() {
        System.out.println(name);
    }
}
```

كل كائن من Person هيبقى عنده نسخته الخاصة من `name`.

---

## 🔸 3. Static Variable (متغير ثابت للكلاس)

- بيتعرف زي instance variable بس قبله `static`.
    
- بيخص الكلاس نفسه، مش كل كائن.
    
- كل الكائنات بتشترك فيه.
    

```java
public class Counter {
    static int count = 0;

    public Counter() {
        count++;
    }
}
```

كل مرة تعمل object جديد من `Counter`، قيمة `count` هتزيد، وكلهم بيشوفوا نفس القيمة.

---

## 🧠 مقارنة سريعة

|النوع|مكان التعريف|مين بيشوفه؟|لازم تهيئة؟|
|---|---|---|---|
|Local|جوه method|بس جوه نفس الـ method|✅ لازم|
|Instance|جوه class|كل object منفصل|❌ لأ|
|Static|جوه class + static|الكلاس وكل الـ objects|❌ لأ|

---

## 🔗 نوتات مرتبطة

- [[Variable Declaration and Initialization]]
    
- [[Constants (final keyword)]]