1**- ONE _ TO _ ONE** 
يعني كل صف في الجدول الأول بيرتبط بصف **واحد فقط** في الجدول التاني، والعكس صحيح.

📌 **مثال:**

- جدول `Users` فيه بيانات المستخدمين.
    
- جدول `UserProfiles` فيه معلومات شخصية زيادة (زي الصورة أو السيرة الذاتية).
    
- كل مستخدم ليه **بروفايل واحد بس**.





2-**One to Many**
- ودي أكتر علاقة مستخدمة.
    
- يعني صف واحد في الجدول الأول بيرتبط بأكتر من صف في الجدول التاني.
    

📌 **مثال:**

- جدول `Departments` فيه الأقسام.
    
- جدول `Employees` فيه الموظفين.
    
- كل قسم فيه **موظفين كتير**، لكن الموظف الواحد **ينتمي لقسم واحد بس**.


3- **Many to Many**
- يعني صف في الجدول الأول ممكن يرتبط بكذا صف في الجدول التاني، والعكس.
    

📌 **مثال:**

- جدول `Students` (الطلاب)
    
- جدول `Courses` (الكورسات)
    
- الطالب ممكن يسجل في كذا كورس، والكورس عليه كذا طالب.
-
 
      هنا بنحتاج جدول تالت نربط بيه، اسمه جدول وسيط (Association Table)
  
  
  **Primary Key (المفتاح الأساسي):** هو العمود اللي بيميز كل صف، لازم يكون فريد (unique).
  
  مثال: `employee_id` في جدول الموظفين.
  
  **Foreign Key (المفتاح الأجنبي):** هو العمود اللي بيربط جدول بجدول تاني.

- مثال: `department_id` في جدول الموظفين، ده بيربطه بجدول الأقسام.
  
  ## 🔁 لما تربط الجداول دي، تقدر تستخدم:

- `INNER JOIN`: ترجع بس الصفوف اللي ليها تطابق.
    
- `LEFT JOIN`: ترجع كل الصفوف من الجدول اللي على الشمال، حتى لو مفيش تطابق.
    
- `RIGHT JOIN`: العكس.
    
- `FULL JOIN`: الاتنين.