# 🧠 Java - JVM, JRE, JDK Concepts

## 🏗️ 1. JDK - Java Development Kit
- ده **أداة المطورين**.
- بيحتوي على:
  - ✅ JRE (Java Runtime Environment)
  - ✅ Compiler (`javac`)
  - ✅ Debugger وأدوات أخرى لكتابة وتشغيل الكود
- 📦 تستخدمه لما تكون "بتكتب" كود Java.

> ✅ JDK = JRE + أدوات التطوير

---

## 🏃‍♂️ 2. JRE - Java Runtime Environment
- ده **بيئة التشغيل** لتشغيل تطبيقات Java الجاهزة.
- يحتوي على:
  - ✅ JVM
  - ✅ مكتبات Java الجاهزة (Standard Libraries)
- لكن **مفيهوش compiler**، يعني مش هينفع تكتب كود جديد.

> ✅ JRE = JVM + Libraries  
> مناسب لو إنت "مستخدم" بتشغّل تطبيق مش بتطوّره.

---

## ⚙️ 3. JVM - Java Virtual Machine
- ده المحرك الأساسي اللي بيشغّل الكود.
- بياخد **Bytecode** الناتج من الـ compiler، وبيشغله على الجهاز.
- كل نظام تشغيل ليه JVM خاص بيه (Windows, Linux...).

> ✅ JVM = بيشغّل الكود بعد الترجمة (Bytecode)

---

## 🔁 العلاقة بينهم:
```text
         [كود Java]
              ↓
        [javac Compiler]
              ↓
          Bytecode (.class)
              ↓
            [JVM]
              ↓
       تشغيل البرنامج فعليًا

```

|العنصر|تستخدمه لما...|يحتوي على|تقدر تكتب كود؟|
|---|---|---|---|
|**JDK**|بتكتب وتطوّر برامج Java|JRE + Compiler وأدوات|✅ نعم|
|**JRE**|بتشغّل برامج Java بس|JVM + Libraries|❌ لأ|
|**JVM**|بتنفيذ الكود|لا يحتوي على أدوات أو مكتبات|❌ لأ|

|الأمر|الوظيفة|
|---|---|
|`javac`|Compile: يترجم الكود|
|`java`|Run: يشغّل البرنامج|


ما هي **الشروط اللي لازم تكون في كلاس Java علشان يشتغل؟**
ال jvm بيدور علي نقطة البدايه 
public static void main(String[] args)
### الشروط:

1. **`public`**: علشان JVM تقدر توصل للدالة من برّه الكلاس
    
2. **`static`**: علشان يتنفذ من غير ما نعمل كائن (object)
    
3. **`void`**: لأن الدالة مش بترجع قيمة
    
4. **`main`**: الاسم اللي JVM بتدور عليه تلقائيًا
    
5. **`String[] args`**: مصفوفة بتستقبل الـ arguments من سطر الأوامر
   
   "الترتيب مش مهم طالما modifiers صح، لكن best practice هو نكتبها `public static void main` علشان ده المتعارف عليه."
   
   
   هل ممكن يكون في أكتر من دالة `main()` في نفس المشروع؟
   
   ممكن يكون فيه **أكتر من `main()`** في المشروع،  
    لكن لازم **كل تشغيل تحدد أنهي كلاس تبدأ منه**.