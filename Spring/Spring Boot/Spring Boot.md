
#### ==**Concept**==
- ده الأساس، وبيشمل الأدوات اللي بتدير كل حاجة في التطبيق، زي **IoC (Inversion of Control)** و **DI (Dependency Injection)**. يعني ببساطة، بدل ما البرنامج يبني الكائنات (objects) بنفسه، Spring هو اللي بيبنيها ويدير العلاقات بينهم.
وهو مبني علي spring core 
مميزاته :-
1- انه stand alone  يعني ممكن اخد منه jar file  واعمله رن في اي حته هيرن بكل مكتباته ومكتباته واعداداته 
2- فيه embedded http  server  الي هو التوم كات يعني مش محتاج تسطبه فا مش محتاجين نdeploy war files 
3- بيوفرلي "starts poms " عشان يسهل عملية ال ميف نكونفيكرشن  يعني بديله ال starts  بتاعت حاجه معينه وهو بينزل المكتبات بتاعتها تلقائي 
 4- بيوفرلي مجموعة من  ال tools  الي اقدر اتيست بيها او اتشيك الهيلثي او الميموري وكده
 5- مش محتاج تعمل xml configuration 
 6- فيه tool CCL  عشان اكتب كومند معينه ترن الحاجات دي 
 عيوبه : 
 بيضيف dependency's  كتير ملهاش لزمه بتكبر حجم البروجكت بتاعك عن طريق starts  عشان كده لازم تبقي aware  بالمكتابات الي بتنزل وتعمل --

---

---
#### **==how to initiate new Spring Boot Project

from yours IDES or 
1- بتروح علي موقع Spring boot )( )
	- [ ] spring.io
	- [ ] project -> spring initializer
	- [ ] choose the required tools 
	- [ ] add dependences that you need --> spring web - sql  - security ---

---
#### **==@SpringBootApplication==** 
هي أنوتيشن واحدة بتختصر 3 أنوتيشنات مهمة في Spring Boot:
@SpringBootApplication
= @Configuration + @EnableAutoConfiguration + @ComponentScan

| الأنوتيشن                  | معناها بالمصري                                                                                             |
| -------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `@Configuration`           | الكلاس ده بيحتوي على Beans وإعدادات للتطبيق                                                                |
| `@EnableAutoConfiguration` | يعني Spring Boot يضبط نفسه تلقائيًا بناءً على اللي موجود في الـ classpath (مثلاً لو لقي Spring MVC أو JPA) |
| `@ComponentScan`           | يدوّر على Beans وأنوتيشنات زي `@Service`, `@Controller` في نفس الباكيج وتحتها                              |

لما بتكتب @SpringBootApplication
```java

public class MyApp {
    public static void main(String[] args) {
        SpringApplication.run(MyApp.class, args);
    }
}

```

يبقى أنت بتقول لـ Spring Boot:
"يا Spring، شغل التطبيق، ضبّط كل حاجة تلقائي، ودوّر على كل الكلاسات اللي عليها أنوتيشنات عشان تخلّيهم Beans!"
- لازم تحط `@SpringBootApplication` في **أعلى مستوى في الباكيج**، عشان `@ComponentScan` تشوف كل حاجة تحتها.
- يعني لازم يبقي فالdeafault package
     
- بتقلل الكود، وبتوفر عليك إعدادات كتير يدوي.
---
### ==**🚀 إيه هو `ApplicationRunner`؟**==
هو **interface** تقدر تنفّذ منه كود معين **أول ما التطبيق يشتغل**، بعد ما Spring يكون خلّص تحميل كل الـ Beans.

يعني تقدر تعتبره زي "دور على الحاجة دي وشغلها أول ما التطبيق يبدأ".
```java 
@Component
public class MyAppRunner implements ApplicationRunner {

    @Override
    public void run(ApplicationArguments args) {
        System.out.println("التطبيق بدأ!");
        if (args.containsOption("mode")) {
            System.out.println("Mode: " + args.getOptionValues("mode"));
        }
    }
}

```
## 🧠 بنستخدمه في إيه؟

- تهيئة بيانات في قاعدة البيانات
    
- تحميل ملفات
    
- تسجيل Logs
    
- اختبار بعض الوظائف
    
- قراءة متغيرات من command line
## إزاي أفعّله؟

بس تحط `@Component` على الكلاس اللي بيطبق `ApplicationRunner`، وSpring هيشغّله تلقائي في الـ startup.

الفرق بين `ApplicationRunner` و `CommandLineRunner`؟

|العنصر|`ApplicationRunner`|`CommandLineRunner`|
|---|---|---|
|بتستقبل إيه؟|`ApplicationArguments` (معالجة الأرجومنتات كويس)|Array of `String` فقط|
|أفضلية الاستخدام|لما تحب تقرأ arguments بشكل structured|لو بس محتاج تقرأ args عادي|