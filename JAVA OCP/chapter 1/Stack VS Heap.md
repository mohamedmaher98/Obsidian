
# 🎯 الأول… لازم تفهم إن JVM بتقسّم الميموري لحتتين رئسيين:

## 1) **Stack Memory**

## 2) **Heap Memory**

وكل واحدة ليها استخدام مختلف و"طبيعة" مختلفة، زي بالظبط لو عندك مكتب شغلك في البيت:

---

# 🟦 1) الـ **Stack** = درج المكتب الصغير

### 📌 تخيّل معايا:

الـ Stack ده "الدرج" اللي قدامك… بتحط فيه الحاجات الصغيرة اللي بتحتاجها بسرعة.

### ✔️ إيه اللي بيتخزن فيه؟

- **المتغيرات البدائية** (int – double – boolean…)
    
- **المراجع فقط** (references) للأوبجكتس اللي موجودة في الـ Heap
    
- **Stack Frames** بتاعت كل ميثود (يعني البيانات اللي تخص الميثود أثناء تشغيلها)
    

### ✔️ أهم صفاته:

- **سريع جدًا**
    
- **حجمه صغير**
    
- **محكوم (LIFO) Last In First Out**
    
- **الميثود أول ما تخلص → الفريم بتاعها يتشال بالكامل**
    
- **مفيش Garbage Collector هنا** (مش محتاج)
    

### 🎯 مثال:

`void test() {     int a = 10;        // a موجودة على ال Stack     Person p = new Person(); // p (الريفيرنس بس) على ال Stack }`

---

# 🟧 2) الـ **Heap** = أوضة التخزين الكبيرة

### 📌 تخيلها:

غرفة كبيرة في البيت بتحط فيها كل العفش الكبير.

### ✔️ اللي بيتخزن فيه:

- **كل الأوبجكتس** (Objects)
    
- **كل الـ Arrays**
    
- **البيانات اللي بتعيش فترة أطول من الميثود**
    

### ✔️ صفاته:

- **أكبر من الـ Stack**
    
- **أبطأ شوية**
    
- **مش محكوم بنظام دخول/خروج معين**
    
- **فيه Garbage Collector**  
    يعني الـ JVM بتنضّفه لما تلاقي أوبجكت مش مستخدم.
    

### 🎯 مثال:

`Person p = new Person();`

- الـ **p** نفسها = على الـ Stack
    
- **new Person()** = على الـ Heap
    

---

# 💡 أهم فرق عملي لازم تفضل فاكره:

|الحاجة|Stack|Heap|
|---|---|---|
|السرعة|أسرع بكتير|أبطأ|
|التخزين|متغيرات بدائية + مراجع|أوبجكتس + Arrays|
|التحكم|LIFO|غير محكوم|
|العمر|يموت مع الميثود|يعيش لحد GC يشيله|
|GC|مفيش|موجود|

---

# 🎯 مثال يثبتلك اللي بيحصل فعلاً:

![https://www.baeldung.com/wp-content/uploads/2018/07/java-heap-stack-diagram.png](https://www.baeldung.com/wp-content/uploads/2018/07/java-heap-stack-diagram.png)

![https://notionalmachines.github.io/assets/images/nm/StackAndHeapDiagram.png](https://notionalmachines.github.io/assets/images/nm/StackAndHeapDiagram.png)

## كود بسيط

`void test() {     int x = 5;               // Stack     int[] arr = new int[3]; // arr reference -> Stack                             // new int[]     -> Heap }`

## اللي بيحصل:

- **x** تخزن على الـ Stack
    
- **arr (الريفيرنس)** على الـ Stack
    
- **الـ array نفسه** على الـ Heap
    
- أول ما الميثود تخلص → Stack Frame كله يتمسح
    
- بس الأوبجكت اللي على الـ Heap ممكن يفضل موجود لو حد تاني ماسكه
    

---

# 🧠 الخلاصة اللي تحفظها:

## 🎯 الـ Stack:

- للحاجات الصغيرة والسريعة
    
- بيموت مع الميثود
    
- محدش بينضفه
    
- السرعة = نور
    

## 🎯 الـ Heap:

- للمعلومات الكبيرة اللي بتعيش فترة
    
- فيه Garbage Collection
    
- أبطأ
    
- ده مكان الأوبجكتس
    

---

# ❓ لو عايز أقفلها بجملة واحدة؟

### ✔️ **Stack = data بتعيش جوه الميثود**

### ✔️ **Heap = data بتعيش بعد الميثود**