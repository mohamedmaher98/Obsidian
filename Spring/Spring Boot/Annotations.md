

عبارة عن metadate  يعني معلومات عن زياده بتوفرها للكوبيلر علي الحاجه الي انت شغال عليها مش بيأثر علي اللوجك الي انت شغال بيه




## 🏷️ أهم Annotations في Spring Boot 

|Annotation|بتعمل إيه؟|بتستخدم فين؟|
|---|---|---|
|`@SpringBootApplication`|علامة بتجمع 3 أنوتيشن: `@Configuration`, `@EnableAutoConfiguration`, `@ComponentScan`|على Main Class بتاع المشروع|
|`@RestController`|بتقول لـ Spring إن الكلاس ده بيقدّم API وبيرجّع JSON|على الكلاس اللي بيخدم REST APIs|
|`@RequestMapping`|بتحدد عنوان URL (Path) لطلب معين|على الميثود أو الكلاس|
|`@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`|بديل مختصر لـ `@RequestMapping(method=...)`|على الميثود|
|`@Autowired`|بتخلّي Spring يحقن (Inject) كائنات تلقائيًا|على الفيلد أو الكونستركتور|
|`@Component`|بتخلي Spring يتعامل مع الكلاس كـ Bean|على أي كلاس|
|`@Service`|نفس فكرة `@Component` لكن للـ Service Layer|على كلاس الخدمات|
|`@Repository`|نفس `@Component` لكن للـ DB Layer وبتضيف معالجات إضافية للأخطاء|على كلاس الـ DAO أو Repository|
|`@Entity`|بتخلي الكلاس يمثل جدول في قاعدة البيانات|على كلاس الـ Model|
|`@Table`|بتحدد اسم الجدول اللي الكلاس بيرتبط بيه|فوق كلاس الـ Entity|
|`@Id`|بتحدد المفتاح الأساسي (Primary Key)|على الفيلد في الكلاس|
|`@GeneratedValue`|بتقول إن القيمة دي تتولد أوتوماتيك (زي الـ ID)|مع `@Id`|
|`@Configuration`|بتقول لـ Spring إن الكلاس ده فيه Beans وتعريفات|على كلاس الإعدادات|
|`@Bean`|بتعرّف Bean يدويًا داخل كلاس فيه `@Configuration`|على ميثود بيرجّع Bean|
|`@Value`|بتقرأ قيمة من `application.properties`|على متغير|
|`@Qualifier`|بتستخدمها لما يكون فيه أكتر من Bean من نفس النوع وعايز تحدد أي واحد تستخدمه|مع `@Autowired`|
|`@Scope`|بتحدد نوع الـ Scope (singleton, prototype, ...)|على Bean أو كلاس|
|`@ComponentScan`|بتقول لـ Spring يدور على Beans في الـ package المعين|غالبًا في Main Class|