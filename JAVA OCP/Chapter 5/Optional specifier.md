## 1) `static`

- ينتمي **للكلاس نفسه** وليس للـ object.
    
- كل الـ objects بيشاركوا نفس النسخة.
    
- يُستخدم مع:
    
    - variables → shared value
        
    - methods → تنادى بدون كائن
        
    - blocks → static initialization
        
- ❌ لا يمكن استخدامه مع:  
    constructors / local methods
---
## 2) `final`

**final = لا تتغير / لا تُعدل**

- **final variable:**
    
    - القيمة تتحدد مرة واحدة فقط.
        
    
    `final int x = 10;`
    
- **final method:**
    
    - لا يمكن عمل override للميثود.
        
- **final class:**
    
    - لا يمكن عمل inheritance منها.

---
## 3) `abstract`

- يستخدم مع:
    
    - **classes** → لا يمكن إنشاء object منها
        
    - **methods** → بدون body، ويجب override في subclass
        
- لو الكلاس فيه abstract method → لازم الكلاس نفسه يبقى abstract.
    

`abstract class Animal {     abstract void eat(); }`

---
## 4) `synchronized`

- يمنع الـ threads من دخول الميثود/البلوك **بنفس الوقت**.
    
- يضمن **thread safety**.
    

`synchronized void update() {}`

---
## 5) `volatile`

- يقول للـ JVM:  
    “القيمة دي اتأكد إنها تتقرأ من الذاكرة الحقيقية، مش الكاش”.
    
- مفيد في multi-threading لمشاركة قيم متغيرة.
- ---

## `transient`

- يمنع الفيلد إنه يتعمله serialization.
    
- يعني مش هيتحفظ لو خزّنت object في ملف أو stream.
    

`transient String password;`

---
## Pass-by-Value VS Pass-by-Reference
## ✅ **الJava بتستخدم Pass by Value فقط**

بس “القيمة” أحيانًا بتكون **Reference** (مرجع لكائن)، وده اللي بيعمل اللخبطة.

## 🔹 ال**Pass by Value (السلوك الفعلي في Java)**

- الميثود بياخد **نسخة من قيمة المتغير**.
    
- لو المتغير primitive → بياخد نسخة من القيمة نفسها.
    
- لو المتغير object → بياخد نسخة من _المرجع_ اللي بيشاور على الكائن.
    
- تغيير المتغير نفسه داخل الميثود **مايأثرش** على المتغير الأصلي.
    
- لكن تعديل **بيانات الكائن** نفسها بيأثر، لأن النسختين بيشاوروا على نفس الكائن.


---
# **1) الفرق بين Local Variable و Instance Variable**

## **Instance Variable**

- بيتعرّف جوا الكلاس (برا الميثود).
    
- بيتعمل **نسخة منه لكل object**.
    
- بيعيش طول ما الـ object عايش.
- ## **Local Variable**

- بيتعرّف جوا **method** أو **block** (زي if أو for).
    
- عمره قصير → بيعيش جوه البلوك بس.
    
- لازم يتعمله **initialize قبل الاستخدام**.

# أولاً: يعني إيه _effectively final_؟

هو متغير **مش مكتوب قبليه `final`**…  
لكن فعليًا **أنت ما غيّرتش قيمته بعد أول مرة**.

يعني المترجم بيعامله _كأنك كاتب عليه final_ لأنه:

- اتعمله **assign مرة واحدة بس**
    
- **ماتغيرش بعد كده**
    

حتى لو **مش مكتوب final** صراحة.

# طب إيه لازمة الـ effectively final؟

عشان حاجتين رئيسيين:

## 1) لازم يكون المتغير final أو effectively final داخل **lambda**

## 2) لازم يكون final أو effectively final داخل **anonymous class**

لو غيرت قيمته → الكومبايلر يرفض.

---
### ✔️ المتغيرات العادية (غير final):

- لو ما اديتهاش قيمة → Java تدي لها default
    
    - int → 0
        
    - boolean → false
        
    - ref → null