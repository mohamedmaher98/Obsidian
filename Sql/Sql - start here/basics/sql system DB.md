# 🧠 SQL Server System Databases – بشرح بلدي

---

## 📋 القواعد النظامية الأربعة:

| القاعدة   | بتعمل إيه؟                                 |
|-----------|---------------------------------------------|
| `master`  | المخ... كل حاجة أساسية عن السيرفر         |
| `model`   | قالب... بيتنسخ منه أي قاعدة جديدة          |
| `msdb`    | المنبه والمنظم... مسؤول عن الشغل التلقائي |
| `tempdb`  | الورقة اللي بنشتغل عليها ونرميها بعد كده  |

---

## 1️⃣ `master` – "عقلك الكبير"

- دي أهم قاعدة في السيرفر كله.
- فيها كل حاجة أساسية:
  - قواعد البيانات اللي عندك
  - اليوزرات والدخول
  - إعدادات السيرفر نفسه
- لو بازت؟ 👎 السيرفر مش هيفتح أصلاً.

> 💡 زي عقل المدير... لو نسي كل حاجة، الشغل كله يقف.

---

## 2️⃣ `model` – "النسّاخ"

- SQL Server بيستخدمها كنموذج.
- كل ما تعمل قاعدة جديدة، السيرفر ينسخ منها.
- لو فيها جدول أو إعداد، بيتنسخ تلقائيًا في أي قاعدة جديدة.

> 💡 زي ملف قالب بتبدأ منه كل مرة تكتب فيه نفس التنسيق.

---

## 3️⃣ `msdb` – "سكرتير المهام"

- مسؤولة عن:
  - تشغيل المهام المجدولة (Jobs)
  - النسخ الاحتياطي (Backup)
  - التنبيهات والسجلات

> 💡 زي سكرتير بيقولك: “الساعة 3 نعمل Backup”، ويسجل كل اللي حصل في دفتره.

---

## 4️⃣ `tempdb` – "ورقة الكشكول المؤقتة"

- دي بيستخدمها السيرفر كمسودة مؤقتة.
- لما تعمل ترتيب، جداول مؤقتة، أو معالجة بيانات كبيرة → هو بيشتغل فيها.
- بتتمسح وتتصفّر كل ما السيرفر يعمل Restart.

> 💡 زي ورقة كشكول بنكتب فيها حسابات ونرميها بعد المذاكرة.

---

## ⚠️ ملاحظات مهمة:

- بلاش تمسح أو تعدّل في أي واحدة من دول من غير ما تكون فاهم كويس.
- دايمًا تابع استهلاك `tempdb` لو حسيت السيرفر تقيل.
- لو `master` حصل فيها تلف، لازم تسترجعها من Backup.

---

## 🧪 للاستعلام عن القواعد دي:

```sql
SELECT name, database_id, state_desc
FROM sys.databases
WHERE database_id <= 4;
