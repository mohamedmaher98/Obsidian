
# 🧲 Static Variables and Methods

## 📝 تعريف بسيط
في Java، الكلمة المفتاحية `static` معناها إن الحاجة دي **مش مرتبطة بكائن معين** (object)،  
لكن مرتبطة بالكلاس نفسه.

يعني المتغير أو الدالة اللي معمولة بـ `static` بيكون فيه منها **نسخة واحدة بس** لكل البرنامج، مش لكل object.

---

## ⚙️ Static Variables (المتغيرات الثابتة)
- بتتعرّف مرة واحدة بس.
- كل الكائنات تشترك فيها.
- بتتخزن في **memory منفصلة** (مش جوه الكائنات).

### 📌 مثال:
```java
public class Student {
    static String schoolName = "Future Academy";
    String name;
}
```

```java
Student s1 = new Student();
Student s2 = new Student();

System.out.println(s1.schoolName); // Future Academy
System.out.println(s2.schoolName); // Future Academy
```

ولو غيّرت القيمة من كائن:
```java
s1.schoolName = "New School";
System.out.println(s2.schoolName); // New School
```

---

## ⚙️ Static Methods (الدوال الثابتة)
- تقدر تنادي عليها من غير ما تعمل object.
- **مينفعش تستخدم فيها instance variables مباشرة**.

### 📌 مثال:
```java
public class MathUtils {
    static int square(int x) {
        return x * x;
    }
}
```

```java
int result = MathUtils.square(5); // 25
```

---

## ⚠️ ملاحظات مهمة
- لو استخدمت `static` مع متغير → كل الكائنات تشترك فيه.
- لو استخدمت `static` مع method → تقدر تنادي عليها من غير object.
- جوه static method مينفعش تستخدم `this` أو أي متغير غير static.

---

## ❓ الفرق بين Static و Instance
| المقارنة           | Static                          | Instance                        |
|--------------------|----------------------------------|----------------------------------|
| ملك مين؟           | الكلاس نفسه                     | كائن (object) معين               |
| النسخ              | نسخة واحدة فقط                  | كل كائن ليه نسخته الخاصة         |
| الاستخدام          | من غير object                   | لازم object                     |
| تقدر تستخدم `this`؟ | ❌                               | ✅                               |

---

## 🧠 استخدامات شائعة لـ static
- الدوال الرياضية (زي `Math.sqrt()`)
- العدادات العامة (زي عدد المستخدمين)
- ال `main` method نفسها → لازم تبقى static عشان Java تبدأ بيها

---

## 🔗 نوتات مرتبطة
- [[Instance Variables and Methods]]
- [[Class Definition]]
- [[this Keyword]]
- [[main Method]]
