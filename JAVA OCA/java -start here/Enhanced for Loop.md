
# 🔁 Enhanced for Loop (for-each)

## 📝 تعريف بسيط
الـ **for-each loop** (أو "enhanced for loop") هو نسخة مبسطة من الـ `for` العادي.  
بيخلّيك تلف على عناصر array أو collection من غير ما تكتب index ولا تشيل هم الطول.

---

## 📌 الصيغة العامة
```java
for (DataType var : arrayOrCollection) {
    // الكود اللي هيتنفذ على كل عنصر
}
```

يعني:  
"لكل عنصر `var` موجود في `arrayOrCollection`، نفّذ الكود ده."

---

## 🧪 مثال مع Array
```java
int[] numbers = {10, 20, 30, 40};

for (int num : numbers) {
    System.out.println(num);
}
```

📤 الناتج:
```
10
20
30
40
```

---

## 🧪 مثال مع ArrayList
```java
import java.util.ArrayList;

ArrayList<String> names = new ArrayList<>();
names.add("Ali");
names.add("Sara");
names.add("Mona");

for (String name : names) {
    System.out.println(name);
}
```

---

## ⚠️ ملاحظات مهمة
- **مش مناسب للتعديل على العناصر.**
  ```java
  for (int num : numbers) {
      num = num * 2; // التعديل ده مش بيأثر على array الأصلية
  }
  ```
- لو عايز تعدل، استخدم `for` العادي مع index.

---

## ✅ مناسب إمتى؟
- لما تكون بس **عايز تقرأ القيم**.
- مش محتاج تعرف رقم العنصر (index).
- وعايز كود بسيط ونظيف.

---

## ❌ مش مناسب إمتى؟
- لو محتاج تستخدم أو تطبع رقم العنصر (index).
- أو تمشي على array بالعكس (من الآخر للأول).
- أو تعدّل على العناصر.

---

|Loop Type|تقدر تعرف الـ index؟|تقدر تعدل القيمة جوه الـ array؟|سهلة في القراءة؟|
|---|---|---|---|
|for العادية|✅ نعم|✅ نعم|😐 عادي|
|for-each|❌ لأ|❌ لأ|✅ جدًا|