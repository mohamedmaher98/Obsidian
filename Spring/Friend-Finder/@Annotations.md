## 📌 يعني إيه `@MappedSuperclass`؟

- ده **أنوتيشن** في JPA بنحطه على **كلاس أب** (superclass) علشان يخلي الكلاسات اللي تورّث منه ترث **الحقول والـ mappings** بتاعته في قاعدة البيانات.
    
- الكلاس اللي عليه `@MappedSuperclass` **مش بيبقى جدول لوحده** في الـ DB.
    
- هو مجرد **قالب** نحط فيه الحقول المشتركة (زي `id`, `createdAt`, `updatedAt`) بدل ما نكررها في كل كلاس.
  
  ## ⚖ الفرق بينها وبين `@Inheritance`

- `@MappedSuperclass` → الحقول بتتنقل لكل جدول، ومفيش جدول للـ superclass.
    
- `@Inheritance` → في جدول للـ superclass، وفيه إستراتيجيات ربط بين الجداول (JOINED, SINGLE_TABLE...).