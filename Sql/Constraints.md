**Constraint** معناها "قيد" أو "شرط" بيتحط على عمود (Column) أو جدول علشان يضمن إن البيانات تكون صح ومافيهاش مشاكل

	 1. `PRIMARY KEY` 
- بيحدد العمود اللي هيمثل الهوية الفريدة لكل صف (Row).
    
- لازم يكون **قيمة مميزة** ومش فاضية (NOT NULL).
  ### 2- `FOREIGN KEY`
- 

- بيربط جدول بجدول تاني. يعني العمود ده بياخد قيمته من عمود في جدول تاني.
  
  ### 3. `UNIQUE`

- بيمنع التكرار في العمود، لكن ممكن يقبل NULL (عكس الـ PRIMARY KEY اللي ما يقبلش NULL).
  
  ### 4. `NOT NULL`

- بيمنع العمود يكون فاضي. لازم تدخل فيه قيمة.
  ### 5- `CHECK`
- 

- بيحدد شرط لازم البيانات تحققّه.
  CREATE TABLE Products (
    id INT PRIMARY KEY,
    price DECIMAL(10,2) CHECK (price > 0)
);
### 6 -`DEFAULT`


- بيدي قيمة افتراضية لو المستخدم مدخلش قيمة.
    

🧩 **مثال**:

sql

CopyEdit

`CREATE TABLE Orders (     id INT PRIMARY KEY,     status VARCHAR(20) DEFAULT 'Pending' );`