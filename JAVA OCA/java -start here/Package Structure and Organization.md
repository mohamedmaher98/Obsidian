
# 📦 Package Structure and Organization

## 📝 تعريف بسيط
في Java، الـ **package** هو مجلد منطّم بيحتوي على الكلاسات اللي ليها علاقة ببعض.  
زي ما بتقسم ملفاتك في فولدرات، Java بتقسم الكود باستخدام packages علشان التنظيم وسهولة الوصول.

---

## 📌 إنشاء Package
لما تيجي تكتب كلاس جوه package، بتحط أول سطر:
```java
package com.myapp.models;
```

وده معناه إن الكلاس موجود جوه هيكل مجلدات كده:
```
com/
 └── myapp/
     └── models/
         └── YourClass.java
```

---

## 📁 هيكل المجلدات الشائع في المشاريع
```
src/
 └── com/
     └── company/
         ├── Main.java
         ├── models/
         │    └── User.java
         ├── services/
         │    └── AuthService.java
         └── utils/
              └── Helper.java
```

---

## 📥 استيراد الكلاسات (import)
لو عايز تستخدم كلاس من package تاني، لازم تعمل `import`:

```java
import com.myapp.models.User;
```

أو تستخدم:
```java
import com.myapp.models.*;
```
لو هتستخدم أكتر من كلاس من نفس الباكدج.

---

## 🔒 أنواع الوصول (Access Modifiers)
- `public`: الكلاس أو الميثود متاح من أي مكان.
- (default): متاح جوه نفس الـ package بس.

---

## ✅ فوائد استخدام Packages
- تنظيم الكود بشكل منطقي.
- تجنب تضارب الأسماء.
- تسهيل الصيانة وإعادة الاستخدام.
- التحكم في الوصول للكود (access control).

---

## 🧠 ملاحظات مهمة
- اسم الباكدج لازم يطابق اسم المجلد على الهارد.
- لو نسيت تكتب `package`، الكلاس هيتحط في الـ **default package**.
- يفضل تسمية الباكدجات باستخدام الدومين معكوس (reverse domain name):
  ```
  com.google.maps
  org.apache.logging
  ```

---

## 🔗 نوتات مرتبطة
- [[Access Modifiers]]
- [[Class Definition]]
- [[Project Structure]]
