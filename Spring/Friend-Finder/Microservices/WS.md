# 1. إيه هو الـ Web Service؟

ببساطة، الـ Web Service هو **برنامج أو خدمة** موجودة على سيرفر، تقدر برامج تانية (Clients) توصل لها وتطلب منها بيانات أو تنفذ عمليات عن طريق **الإنترنت** أو الشبكة، باستخدام بروتوكولات قياسية.

يعني بدل ما تبرمج نفس الوظيفة في كل تطبيق، بتعملها كـ "خدمة" وتخلي أي حد (برنامج أو موبايل أو موقع) يقدر يستخدمها.

### 2. الهدف الرئيسي

- **التكامل (Integration)**: تخلي أنظمة مختلفة تتكلم مع بعض حتى لو مكتوبة بلغات مختلفة (Java، Python، .NET...).
    
- **إعادة الاستخدام (Reusability)**: تعمل الخدمة مرة واحدة وتستخدمها في كذا مشروع.
    
- **المرونة**: أي تحديث في الخدمة بيكون مركزي، والـ Clients ما تحتاجش تعدل إلا لو تغير الـ API.

### 3. المكونات الأساسية لأي Web Service

- ا**لClient**: اللي بيطلب الخدمة (تطبيق موبايل، موقع ويب، برنامج).
    
- ال**Server**: اللي بيستضيف الخدمة وينفذ الطلب.
    
- ال**Request & Response**: الرسائل اللي بتتنقل بين الـ Client والـ Server (غالباً JSON أو XML).
    
- ا**Protocol**: اللغة أو القواعد اللي بيتكلموا بيها (غالباً HTTP).

### 4. مثال بسيط

تخيل إنك فاتح تطبيق الطقس على موبايلك:

- التطبيق (Client) بيبعت طلب لسيرفر الطقس (Web Service).
    
- السيرفر بيرجع بيانات الطقس بصيغة JSON.
    
- التطبيق يعرض البيانات على الشاشة.


# 2-  البروتوكولات وصيغ البيانات

## 1. البروتوكول الأكثر استخدامًا: **HTTP**

هو نفس البروتوكول اللي بيشغّل الويب العادي (المواقع)، لكن هنا بنستعمله علشان نخلي البرامج تتكلم مع بعض.

- الطلب بيبقى عبارة عن **HTTP Request** (مثلاً GET أو POST).
    
- الرد بيبقى **HTTP Response** فيه **status code** وبيانات (body).

  
## أشهر أنواع البيانات اللي بيتبادلها الـ Web Service

### ال**JSON** (JavaScript Object Notation)

- شكل بيانات خفيف وسهل القراءة للبشر والآلات.
### ال**XML** (eXtensible Markup Language)

- أقدم من JSON، ولسه مستخدم في بعض الأنظمة (خصوصًا SOAP).
## 3. الContent-Type

في أي Web Service، لازم نحدد نوع البيانات اللي جايه أو راجعة. ده بيكون في **HTTP Header**:

- لو JSON:  
    `Content-Type: application/json`
    
- لو XML:  
    `Content-Type: application/xml`
    
    # **3 — أنواع الـ Web Services**.
## 1. ال**SOAP Web Service** (Simple Object Access Protocol)

- قديم شوية لكن لسه مستخدم في أنظمة المؤسسات الكبيرة (Banks, ERPs...).
    
- بيعتمد على **XML** بشكل أساسي في الرسائل.
    
- بيشتغل غالبًا فوق HTTP، لكن ممكن كمان فوق بروتوكولات تانية زي SMTP.
    
- بيستخدم **WSDL** (Web Services Description Language) علشان يوصف الخدمة:
    
    - بيحدد العمليات (methods) اللي موجودة.
        
    - بيحدد شكل الرسائل وبيانات الدخول والخروج.
        
- مميزاته: قوي، فيه أمان متقدم، مناسب للمعاملات المعقدة.
    
- عيوبه: تقيل ومعقد، وصعب على المبتدئين.

مثال
```xml
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <GetWeather>
      <City>Cairo</City>
    </GetWeather>
  </soap:Body>
</soap:Envelope>
```
---
## 2. ال**RESTful Web Service** (Representational State Transfer)

- الأشهر حاليًا، وخفيف وسهل.
    
- بيعتمد على بروتوكول HTTP بشكل مباشر.
    
- بيستخدم **HTTP Methods** زي GET, POST, PUT, DELETE.
    
- البيانات غالبًا بتكون **JSON**، لكن ممكن XML أو أي حاجة.
    
- بيعتمد على **Resources** (زي `/users`, `/products`).
    
- مميزاته: سهل، سريع، وبيشتغل على أي لغة أو منصة.

```json
GET /weather?city=Cairo HTTP/1.1
Host: api.example.com
Accept: application/json
```

## 3. لا**gRPC** (للإشارة فقط)

- أحدث من SOAP وREST.
    
- بيستخدم **Protocol Buffers** بدل JSON/XML.
    
- سريع جدًا، مناسب للـ microservices والاتصال بين السيرفرات.

💡 **الخلاصة**:

- ال**SOAP**: استخدمه لو في بيئة Enterprise أو لازم أمان/معايير معقدة.
    
- ال**REST**: الافتراضي دلوقتي لأي Web API حديث.
    
- ال**gRPC**: ممتاز للـ microservices أو لما تحتاج performance عالي جدًا.

---
# 4 — مبادئ REST الأساسية

## 1. الResource-Oriented Design (تصميم على أساس الموارد)

في REST، كل حاجة بنمثلها كـ **Resource**.

- **المورد (Resource)**: كيان له بيانات وسلوك (زي "User"، "Product"، "Order").
    
- بنعبر عنه بـ **URI** واضح:
    
    ال- `/users` → كل المستخدمين
        
    ال- `/users/1` → المستخدم رقم 1
        
    ال- `/products/5/reviews` → الريفيوز لمنتج رقم 5
        

**أفضل ممارسات:**

- استخدم **أسماء جمع** (`/users` مش `/getUser`).
    
- خلي الـ URI يعبر عن الكيان مش الفعل (الفعل بيتحدد بالـ HTTP Method).
---
## 2. HTTP Methods (الأفعال)

REST بيعتمد على أفعال HTTP بدل ما نكتب الفعل في اسم الـ endpoint:

- **GET** → جلب بيانات (Read).
    
- **POST** → إنشاء مورد جديد (Create).
    
- **PUT** → تحديث مورد بالكامل (Update).
    
- **PATCH** → تحديث جزئي (Partial Update).
    
- **DELETE** → حذف مورد.
---
## 3.لب Statelessness (بدون حالة)

- كل طلب (Request) لازم يحتوي على كل المعلومات اللي السيرفر يحتاجها لمعالجته.
    
- السيرفر ما يخزنش "session state" بين الطلبات (في REST الكلاسيكي).
    
- ده بيخلي الخدمة أسهل في التوسع (scalability).
---
## 4. الHTTP Status Codes (أكواد الحالة)

من الحاجات اللي بتميز REST إنك لازم تستخدم الأكواد صح:

- **200 OK** → العملية نجحت.
    
- **201 Created** → إنشاء مورد جديد.
    
- **204 No Content** → العملية نجحت لكن مفيش رد.
    
- **400 Bad Request** → الطلب فيه خطأ (validation error).
    
- **401 Unauthorized** → محتاج تسجيل دخول.
    
- **403 Forbidden** → ممنوع حتى لو مسجل دخول.
    
- **404 Not Found** → المورد مش موجود.
    
- **500 Internal Server Error** → مشكلة في السيرفر.
---
## 5.ال Content Negotiation

- العميل يحدد في الـ header هو عايز إيه:
    
    - `Accept: application/json`
        
    - `Accept: application/xml`
        
- السيرفر يرد بنفس النوع لو ممكن.
---
## 6.ال Versioning

لما تكبر الـ API وعايز تغيرها بدون ما تكسر النسخ القديمة، بتعمل version:

- `/api/v1/users`
    
- `/api/v2/users`

---
## 7. Pagination & Filtering

لما المورد فيه بيانات كتير، لازم تقسم النتايج:

- `/users?page=2&size=20`
    
- `/products?category=books&sort=price_desc`
---
## 8. الError Handling

في الـ REST المحترف، الرد على الأخطاء بيكون structured:


```json
{   
	"error": "Validation Failed",
	   "details": [
	        { "field": "email", "message": "Invalid email format" }
	    ]
}
```
