تخيّل إن عندك **عمارة فيها شقق مرقّمة**  
(0، 1، 2، 3…)

والشقق دي هي اللي بنحط فيها البيانات.

> **الـ Array = عمارة ثابتة عدد الشقق بتاعتها  
> وكل شقة لها رقم (index) بنعدل بيه ونشوف بيه البيانات.**

أهم صفات الـ Array

1) **Fixed Size — حجمها ثابت**
2)  **Ordered — مترتّبة**
3) Homogeneous — نفس النوع
   يعني العمارة كلها لنوع واحد:

- Array للـ int
    
- Array للـ String
    
- Array للـ Person
    

لكن **مش ينفع تخلط أنواع جوه نفس العمارة**.

4) **Indexed Access — الوصول بالرقم**

طرق إنشاء الـ Array

int[] arr = new int[3];
int[] arr = {10, 20, 30};

Arrays of Objects
مش لازم يكون النوع primitive
نقدر نعمل Array من كائنات:
Person[] people = new Person[3];
people[0] = new Person("Ali");
people[1] = new Person("Sara");

# 🔥 Array vs ArrayList؟

تشبيه بسيط:

## Array = عمارة ثابتة وعدد الشقق ثابت
# خلاصة مستحيل تنساها:

- Array = شكل ثابت
    
- index يبدأ من 0
    
- نفس النوع
    
- أسرع من List في الوصول المباشر
    
- لكن مش مرنة في الحجم