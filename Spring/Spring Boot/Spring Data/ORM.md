## 🔄 يعني إيه ORM؟

كلمة **ORM** اختصار لـ:
 **Object-Relational Mapping**

يعني ببساطة:

**تحويل الجداول في قاعدة البيانات لكلاسات Java، وكل عمود بيتحول لفيلد (متغير).**

## 📦 ليه نستخدم ORM؟

بدل ما نكتب SQL يدويًا كل مرة، ORM بيخلينا نكتب كود Java عادي، وهو يتصرف ويتعامل مع DB.

يعني بدل ما تعمل:
SELECT * FROM employees WHERE id = 1;
ex
Employee emp = employeeRepository.findById(1L).get();

🧱 أشهر أدوات ORM:

|الأداة|بتشتغل فين؟|
|---|---|
|**Hibernate**|الأشهر في Java وSpring|
|**JPA**|Standard interface للـ ORM (و Hibernate بيطبقها)|
|**Spring Data JPA**|بتبسط استخدام JPA/ORM أكتر|

🧪 مثال بسيط (Hibernate / JPA):
1. الكلاس (Entity):
```java
@Entity
@Table(name = "employees")
public class Employee {
    @Id
    @GeneratedValue
    private Long id;

    private String name;
    private double salary;
}
// in repo 
@Repository
public interface EmployeeRepository extends JpaRepository<Employee, Long> {
}

```

مميزاته 

|الميزة|الشرح|
|---|---|
|توفير الوقت|مش محتاج تكتب SQL يدوي كتير|
|كود أوضح|شغال بأوبجكتات بدل Queries|
|إدارة العلاقات|يقدر يتعامل مع العلاقات بين الجداول|
|Transaction Management|بيساعد في إدارة المعاملات بشكل كويس|
عيوبه 

| العيب        | الشرح                         |
| ------------ | ----------------------------- |
| أبطأ أحيانًا | ممكن يبقى أبطأ من SQL المباشر |
| التعلم       | محتاج فهم كويس للـ mapping    |
| مشاكل الكاش  | ممكن يعمل مشاكل لو مش متظبط   |