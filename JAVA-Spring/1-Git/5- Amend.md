## 1️⃣ يعني إيه `--amend` أصلاً؟

كلمة **amend** يعني _يعدّل_.

في Git:

> `git commit --amend`  
> يعني **عدّل آخر commit عملته** بدل ما تعمل commit جديد.

تخيل إنك:

- عملت commit
    
- وبعدها على طول اكتشفت إنك:
    
    - نسيت ملف
        
    - أو كتبت رسالة commit غلط
        
    - أو محتاج تزبط حاجة بسيطة
        

بدل ما تزود commit جديد ويلخبط التاريخ → **تعدّل الأخير وخلاص**.

git commit --amend


## 3️⃣ إضافة ملفات نسيتها في آخر commit

ده أشهر استخدام 👌

السيناريو:

- عدلت ملفات
    
- عملت commit
    
- بعدها اكتشفت إنك نسيت ملف 🤦‍♂️
    

الحل:

`git add file_i_forgot.java git commit --amend`

Git هيعمل إيه؟
- ياخد **الـ commit القديم**
    
- يضيف عليه الملف الجديد
    
- يطلع commit جديد بنفس الفكرة
    
- القديم يتشال من التاريخ
    

⚠️ مهم:  
الـ commit hash بيتغير (وده طبيعي).

## 6️⃣ حاجة خطيرة شوية ⚠️ (بس مهمة)

### ❌ متستخدمش amend بعد ما تعمل push



## 7️⃣ سيناريوهات حقيقية من الشغل

### ✔️ صح:

- typo في commit message
    
- نسيت import
    
- نسيت ملف config
    
- تعديل بسيط في logic
    

### ❌ غلط:

-ال commit متشارك مع الفريق
    
- بعد code review
    
- على branch رئيسي زي `main` أو `develop`


## 8️⃣ مثال كامل من الأول للآخر

`git add .
git commit -m "add employee service"`

اكتشفت إنك نسيت ملف:

`git add EmployeeMapper.java git commit --amend`

النتيجة:  
✔️ commit واحد  
✔️ تاريخ نضيف  
✔️ مفيش commit زيادة مالوش لازمة

---

## 9️⃣ خلاصة بلدي

- ال`--amend` = تمسح آخر commit وتعيده مظبوط
    
- مفيد جدًا للتنضيف
    
- خطر لو استخدمته بعد الـ push
    
- استخدمه وانت لسه لوحدك على الـ commit