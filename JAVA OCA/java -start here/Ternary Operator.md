# Ternary Operator

## 📝 تعريف بسيط

الـ **Ternary Operator** هو طريقة مختصرة لكتابة شرط `if-else` في سطر واحد.

📌 شكله العام:

```java
condition ? value_if_true : value_if_false;
```

يعني: لو الشرط صح → خُد القيمة الأولى، لو غلط → خُد القيمة التانية.

---

## 🧪 مثال بسيط

```java
int age = 20;
String result = (age >= 18) ? "مسموح" : "ممنوع";
System.out.println(result); // مسموح
```

نفس الكود ده كان ممكن يتكتب بـ if-else كده:

```java
String result;
if (age >= 18) {
    result = "مسموح";
} else {
    result = "ممنوع";
}
```

---

## ✅ مثال تاني

```java
int a = 10, b = 20;
int max = (a > b) ? a : b;
System.out.println("Max = " + max); // Max = 20
```

---

## ⚠️ ملاحظات

- ternary مفيد لو عايز تاخد قرار بسيط وتخزن القيمة في متغير.
    
- ما تستخدموش لو الشرط معقد أو فيه أكتر من خطوة علشان الكود مايبقاش صعب القراءة.
    
- تقدر تداخل ternary داخل ternary (لكن بحذر):
    

```java
int x = 5;
String result = (x > 0) ? (x > 10 ? "أكبر من 10" : "بين 1 و 10") : "صفر أو أقل";
```

---

## 🔗 نوتات مرتبطة

- [[Control Flow (if, else, switch)]]
    
- [[Comparison Operators]]
    
- [[Logical Operators]]