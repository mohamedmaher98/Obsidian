## 🔍 مكونات الكلاس:
في Java، الـ **class** هو البنية الأساسية اللي بتبني بيها الكود بتاعك.
تقدر تعتبره **قالب (template)** بيعرّف الشكل والسلوك (attributes & methods) للكائنات اللي هتتعمل منه.
- **Fields / Attributes:** المتغيرات اللي بتخزن حالة الكائن.
    
- **Methods:** الدوال اللي بتحدد سلوك الكائن.
    
- **Constructors:** بنستخدمهم علشان نجهّز الكائن وقت إنشاؤه.
    
- **Access Modifiers:** زي `public`, `private`, `protected` لتحديد الوصول للكود.
  
  ## 🧠 ملاحظات مهمة:

- ممكن يكون عندك أكتر من كلاس في نفس الملف، بس لازم واحد بس يكون `public` واسم الملف يكون زيه.
    
- لو ما كتبتش `public`, بيكون الكلاس `package-private` (يعني مسموح الوصول ليه داخل نفس الباكدج فقط).