ده معناه اني استخدم كلاس عشان يخزن الداتا
### ==**JAR - WAR - EAR**==
عبارة عن الملفات التي يتم إنشاؤها في عملية بناء التطبيق

**JAR (Java Archive)**:

==**الـJAR**==   
(java archive)

- **الهدف**: هو ملف مضغوط يحتوي على ملفات `.class` (التطبيقات أو الكود البرمجي) والموارد مثل الصور، الصوتيات، وملفات الإعدادات الخاصة بالتطبيق.
- **الاستخدام**: يُستخدم لتغليف التطبيقات أو المكتبات الصغيرة التي يمكن استيرادها واستخدامها في برامج Java أخرى.
- **الهيكل**: يحتوي على الملفات البرمجية `.class` وأحيانًا ملفات الموارد مثل `properties` وملفات التوثيق.
- **المزايا**:
    -يمكن تشغيله من خلال الأمر `java -jar`.
    - يسهل توزيع الكود.
    - يدعم التوقيع الرقمي للحفاظ على أمان الملف.
- **مثال**: مكتبة Java التي يمكن تضمينها في مشروع آخر.
- 
==الWAR==
- **WAR (Web Archive)**:
    
    - **الهدف**: هو ملف مضغوط يُستخدم لتغليف تطبيقات الويب الخاصة بـ Java.
    - **الاستخدام**: يُستخدم لتوزيع تطبيقات الويب مثل تطبيقات JSP (Java Server Pages) وServlets.
    - **الهيكل**: يحتوي على:
        - مجلد `WEB-INF` الذي يتضمن ملفات مثل:
            - `web.xml` (ملف التكوين للتطبيق).
            - ملفات `.class` الخاصة بـ Servlets أو أي كود Java آخر.
            - مكتبات JAR المستخدمة في التطبيق.
    - **المزايا**:
        - يمكن نشره على خوادم التطبيقات مثل Tomcat أو JBoss.
        - يحتوي على جميع المكونات التي يحتاجها التطبيق للعمل.
                - **مثال**: تطبيق ويب مبني بـ Java يستخدم JSP و Servlets.
- ==**EAR (Enterprise Archive):**==
- 
    - **الهدف**: هو ملف مضغوط يُستخدم لتغليف تطبيقات Java Enterprise التي تضم أكثر من تطبيق أو جزء واحد (مثل تطبيق ويب وقاعدة بيانات).
    - **الاستخدام**: يُستخدم لتغليف تطبيقات المؤسسات الكبيرة، ويحتوي عادة على تطبيقات Java EE (مثل EJB, Web Services).
    - **الهيكل**: يحتوي على:
        - ملفات WAR التي تمثل تطبيقات الويب.
        - ملفات JAR التي تحتوي على الكود البرمجي أو المكتبات المشتركة.
        - ملف `application.xml` الذي يحتوي على تكوينات التطبيق.
    - **المزايا**:
        - يدعم تنفيذ التطبيقات متعددة الطبقات.
        - يمكن نشره على خوادم Java EE مثل WebLogic أو JBoss.
    - **مثال**: تطبيق يحتوي على واجهة ويب (WAR) وقاعدة بيانات (JAR) وتنفيذ الأعمال (EJB).
---
- ملخص:
    - ملف مضغوط يحتوي على ملفات `.class` والموارد مثل الصور والصوت.
    - يستخدم لتوزيع المكتبات أو التطبيقات الصغيرة.
    - يمكن تشغيله باستخدام `java -jar`.
        
- **WAR (Web Archive)**:
    - ملف مضغوط يُستخدم لتغليف تطبيقات الويب (مثل JSP و Servlets).
    - يحتوي على `WEB-INF` مع ملفات التكوين والملفات البرمجية.
    - يُنشر على خوادم مثل Tomcat و JBoss.
- **EAR (Enterprise Archive)**:
    
    - ملف مضغوط يحتوي على تطبيقات المؤسسات (Java EE).
    - يتضمن ملفات WAR و JAR مع تكوينات في `application.xml`.        
    - يُنشر على خوادم Java EE مثل WebLogic أو JBoss.
---
### ==**Framework**== 
**الإطار العمل (Framework)** هو مجموعة من الأدوات، المكتبات، والأدلة التي توفر بيئة تطوير مُهيكلة لبناء التطبيقات. بالإجمال، هو حل شامل يوفر لك البنية الأساسية التي تحتاجها لبناء تطبيقات مع تفاعلات محددة. الفريمورك يساعد في تسريع تطوير البرمجيات ويجعلها أكثر استقرارًا وقابلية للصيانة.
- **الفريمورك (Framework)** هو مجموعة من الأدوات والمكتبات التي توفر بيئة عمل لتطوير التطبيقات.
    
- **المميزات**:
    - يوفر بنية منظمّة.  
    - يعزز إعادة استخدام الكود.    
    - يدعم التكامل مع الأدوات الأخرى.    
    - يوفر مزايا الأمان.    
- **أنواع الفريمورك**:
    
    - **Web Frameworks** (مثل Spring, Django).
    - **Mobile Frameworks** (مثل React Native, Flutter).
    - **Frontend Frameworks** (مثل React.js, Angular).
    - **Database Frameworks** (مثل Hibernate).
    - **Enterprise Frameworks** (مثل Spring Boot).
- **الفوائد**: تسريع التطوير، الكفاءة، سهولة الصيانة.
---
### ==**الفرق بين Singleton و Prototype في Spring**==


|الخاصية|Singleton (الافتراضي) 🟢|Prototype 🔄|
|---|---|---|
|عدد النسخ (Instances)|نسخة واحدة لكل الـ ApplicationContext|نسخة جديدة كل ما تطلب Bean|
|الاستخدام الشائع|الخدمات اللي مفيهاش حالة (Stateless)|الكلاسات اللي محتاجة حالة جديدة كل مرة (Stateful)|
|الإنشاء|Spring بينشئه مرة واحدة ويخزّنه|Spring بينشئ نسخة كل مرة بـ `getBean()`|
|الأداء|أسرع (لأنها نسخة واحدة)|أبطأ شوية (لأن كل مرة بينشئ جديد)|
|Lifecycle control|Spring بيتحكم فيه بالكامل|بعد ما ينشئه، أنت المسؤول عن تدميره|

 إمتى تستخدم كل واحد؟

ال 🟢 Singleton (الافتراضي)

- لو الكلاس **Stateless** (يعني مش بيحتفظ بحالة أو بيانات مختلفة لكل مستخدم).
    
- مثال: `EmployeeService`, `UserRepository`, `OrderValidator`.
    

```java
@Service
public class EmployeeService {
    // نفس النسخة بتستخدم طول عمر التطبيق
}
```

==ال Prototype==

- لو الكلاس **Stateful** (كل طلب محتاج نسخة مختلفة ببياناته).
    
- مثال: `ReportBuilder`, `TempInvoice`, `RequestData`.

```java
@Component
@Scope("prototype")
public class ReportBuilder {
    // كل مرة تستخدمه Spring هيعمل نسخة جديدة
}
```

مثال   : : 

```java
@Component
@Scope("singleton") // أو @Scope("prototype")
public class MyBean {
    public MyBean() {
        System.out.println("تم إنشاء MyBean");
    }
}
@Component
public class TestBean {

    @Autowired
    private ApplicationContext context;

    public void test() {
        MyBean b1 = context.getBean(MyBean.class);
        MyBean b2 = context.getBean(MyBean.class);

        System.out.println(b1 == b2); // true في Singleton، false في Prototype
    }
}

```