إيه هو **Dependency Injection (DI)** في Spring؟
ببساطة كده، **Dependency Injection** يعني إنك ما تعملش كائن جوه الكلاس بنفسك، بل تخلي Spring هو اللي "يحقن" (inject) الكائنات المطلوبة أو الاعتماديات (dependencies) في الكلاس بتاعك.
ليه بنستخدم Dependency Injection؟
لأنها:

- بتخلي الكود **أنضف وأسهل في الصيانة**.
    
- بتفصل الكلاسات عن بعضها (Loose Coupling).
    
- بتخلي الكود أسهل في **الاختبار (Testing)** لأنك تقدر تبدل الكائنات اللي بتستخدمها بـ mock بسهولة.

أنواع Dependency Injection في Spring:

Constructor Injection
الاعتماديات بتدخل عن طريق الـ constructor
Setter Injection
الاعتماديات بتدخل عن طريق setter methods
Field Injection
الاعتماديات بتدخل مباشرة على المتغيرات باستخدام @Autowired

==ملخص== 
****
- و**Dependency Injection (DI)**: طريقة لتوفير الاعتماديات للكائنات بدل ما ينشئوها بنفسهم.
    
- **الهدف**: تقليل الترابط القوي (loose coupling)، سهولة الاختبار، تنظيم الكود.
    
- **أنواع DI في Spring**:
    
    - Constructor Injection.
        
    - Setter Injection.
        
    - Field Injection.
        
- و**Spring** بيستخدم IoC Container عشان يعمل Injection تلقائيًا باستخدام أنوتيشن زي `@Autowired` أو `@Inject`.

---
### ==**Bean Scopes**==
في Spring، كل Bean بيكون ليه "scope"، يعني **مدى الحياة بتاعه داخل الـ container**، وده بيحدد إذا كان الـ Bean بيتعمل منه نسخة واحدة ولا أكتر، وبيفضل عايش قد إيه

## 🧠 Bean Scopes في Spring

| النوع | المعنى | متى يتم إنشاء الـ Bean؟ | عدد النسخ |
|------|--------|---------------------------|-----------|
| **singleton** (افتراضي) | نسخة واحدة فقط | أول ما Spring يبدأ | نسخة واحدة لكل الـ container |
| **prototype**           | كل مرة Bean جديد | كل مرة تطلب الـ Bean | عدد لا نهائي |
| **request**             | لكل HTTP request نسخة | مع بداية كل request | لكل طلب نسخة |
| **session**             | لكل Session نسخة | مع بداية الـ session | لكل session نسخة |
| **application**         | نسخة واحدة لتطبيق الويب كله | مع بداية التطبيق | نسخة واحدة |
| **websocket**           | نسخة لكل WebSocket | مع كل connection | واحدة لكل WebSocket |
- **singleton (افتراضي)**: نسخة واحدة فقط لكل التطبيق.
    
- **prototype**: كل مرة Bean جديد.
    
- **request**: نسخة لكل HTTP request (تطبيقات ويب).
    
- **session**: نسخة لكل Session (تطبيقات ويب).
    
- **application**: نسخة واحدة لكل تطبيق ويب.
    
- **websocket**: نسخة لكل WebSocket session.
    

> استخدم `@Scope("...")` مع `@Component` أو `@Bean` لتحديد نوع السكوب.

---
### ==**Bean life Cycle**==

في Spring، أي Bean بيمر بمراحل معينة من لحظة ما بيتولد لحد ما يتم تدميره.  
الـ Spring Container هو المسؤول عن إدارة دورة الحياة دي. 

 المراحل الأساسية:

1. **Instantiation (الإنشاء)**  
    Spring بينشئ كائن جديد من الكلاس (Bean).
    
2. **Populate Properties (حقن القيم)**  
    Spring بيعمل Dependency Injection ويملي المتغيرات اللي معمولها Inject.
    
3. **BeanNameAware**  
    لو الكلاس بيطبّق `BeanNameAware`، Spring بيبعتله اسمه.
    
4. **BeanFactoryAware / ApplicationContextAware**  
    Spring يقدر يدي للـ Bean الـ Container نفسه لو الكلاس بيطبّق الواجهات دي.
    
5. **Pre-initialization (قبل التهيئة)**  
    أي BeanPostProcessor شغّال بيبدأ يشتغل على الـ Bean قبل ما يتجهّز
    